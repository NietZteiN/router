# tofu_sisa_lora runtime dependencies — curated from the cluster's `test-env` (Python 3.12).
#
# Install into a Python 3.12 env, e.g.:
#     conda create -n test-env python=3.12 && conda activate test-env
#     pip install -r requirements.txt
#
# NOTES for a puller on a different machine:
#  * torch is pinned to the CUDA 12.1 wheel (2.5.1+cu121). On a different CUDA/CPU box, install the
#    matching torch build instead (see https://pytorch.org) — e.g. `pip install torch==2.5.1`.
#  * The plotting scripts (plot_*.py) do NOT run in this env — matplotlib is intentionally absent here.
#    Run those under base anaconda Python 3.13 (`/home/jack/anaconda3/bin/python`), which has matplotlib.
#  * `nevergrad` is only needed for the `lorahub` merge method; `bitsandbytes` only for 8-bit paths.

torch==2.5.1+cu121
transformers==4.48.3
tokenizers==0.21.4
peft==0.14.0
trl==0.9.6
accelerate==1.13.0
datasets==4.8.5
huggingface_hub==0.36.2
safetensors==0.7.0

numpy==1.26.4
scipy==1.17.1
scikit-learn==1.9.0
pandas==3.0.3

evaluate==0.4.6
rouge_score==0.1.2
nltk==3.9.4
sentence-transformers==3.4.1

nevergrad==1.0.12      # lorahub merge only
optuna==4.9.0          # hyperparameter search only; pulls in sqlalchemy/alembic. Added 2026-08-20.
bitsandbytes==0.49.2   # optional (8-bit)
tqdm==4.67.3
rich                   # undeclared transitive dep of trl 0.9.6 (trl/trainer/utils.py imports it);
                       # absent from the original curated list, so a clean install of this file
                       # fails at `from trl import SFTTrainer`. Added 2026-07-24 (CISPA rebuild).
