{
  "_note": "P2a shared-block pretrain (phase 0): LM with REAL answer labels on the author-free pool - the first 2000 rows of the seed-42 Alpaca shuffle + all 100 TOFU real_authors rows; NEVER any of the 200 TOFU authors (deliberate divergence from the design doc's retain+generic pool: retain rows would contaminate the undeletable shared block) and never the guarded control split. Output runs/phase0_s42/blocktc.pt is the phase0_checkpoint of EVERY phase-1 config (pilot arms AND K=200 - same full 200-block topology everywhere, tc_sha-pinned). epochs/lr are deliberately un-swept (the pilot sweeps phase-1 lr/lambda only; ~66 steps/epoch at bs32 over the 2100-row pool). lambda_max is unused in phase 0 (suppression is phase-1-only) and set 0 to say so; authors_subset/detector_init are ignored in phase 0 (detector init happens at phase-1 start, on top of this checkpoint's seeded author rows).",
  "model": "meta-llama/Llama-3.2-1B-Instruct",
  "insert_layer": 9,
  "span": 3,
  "m_author": 32,
  "m_shared": 128,
  "n_authors": 200,
  "authors_subset": null,
  "seed": 42,
  "max_length": 512,
  "batch_size": 32,
  "grad_accum": 1,
  "epochs": 15,
  "lr": 0.001,
  "lambda_max": 0.0,
  "lambda_warmup_frac": 0.15,
  "clip_norm": 1.0,
  "detector_init": "questions",
  "init_scale": 1.0,
  "alpaca_n": 2000,
  "phase": "phase0",
  "phase0_checkpoint": null,
  "run_name": "phase0_s42"
}
