### Target Date: 2026-07-08 (why is the isolated adapter weak? rank vs undertraining — design + pre-registration)
- **Hypotheses / what we're testing:** Exp-5b found the isolated per-author adapter reaches only subset retain_prob 0.399 vs joint-ft 0.924 on the same rows ([results](2026-07-08_subset-utility-results.md)). Two candidate explanations, two cheap tests on author 82:
  - **H6 (capacity is not the constraint / "LoRA overkill" is capacity-only):** own-author recall is rank-independent — the existing r1 and r8 per-author adapters (`_k200_r{1,8}_e5_lr1e4/shard_82`, same recipe otherwise) score within noise of r32 (subset retain_prob 0.399, forget_rouge 0.510). CONFIRM: |Δprob| ≤ 0.05 across r1/r8/r32. REFUTE: recall rises materially with rank (capacity-bound) or r1 collapses (capacity floor inside 1..8).
  - **H7 (undertraining dominates):** the frozen recipe gives a 20-example shard only ~6–10 optimizer steps (batch 4 × accum 4, 5 epochs); training the same r32 adapter for 25 and 50 epochs closes a large part of the iso→ft gap. CONFIRM: e50 subset retain_prob ≥ 0.6 (≥ ~40% of the 0.399→0.924 gap closed). REFUTE: e50 ≈ e5 (then the deficit is structural — no shared-format amortization — not step-count).
- **Setup:** GPU budget capped at 4 (user constraint): 2 training jobs (e25/e50) + eval array `%2`, then 4 follow-up evals `%4`. Trainings: `train_lora_shard.py --shard_id 82 --k 200 --model_name meta-llama/Llama-2-7B-chat-hf --epochs {25,50} --output_dir /storage2/.../Llama-2-7B-chat-hf_iso82_e{25,50}` (all other flags = frozen-recipe defaults r32/α64/lr1e-4/seed 42, rslora). Evals reuse the nmerge pipeline via a custom manifest (`EVAL_MANIFEST` override in `submit_nmerge.sh`): per variant (r1, r8, e25, e50) one probe row (`--eval_shard_id 82` → forget_rouge/ppl on author 82) + one subset row (`--retain_author_ids 82`, forget 199 → retain_prob/rouge/ppl on author 82's rows), smoke caps, results into `Llama-2-7B-chat-hf_nmerge_r32/results/smoke/` as `iso_a82_{r1,r8,e25,e50}__{own82,subset}.json`. References already on disk: r32/e5 iso probe (rouge 0.5102, ppl 3.56) + subset (prob 0.3991); ft_r32_sub8 ceiling 0.9237; base floor 0.1703. Job IDs in the results entry.
- **Results:** [pending]
- **What worked / hypothesis verdict:** [pending]
- **Observations:** [pending]
- **New questions / new hypotheses:** [pending]
- **Next Steps:** trainings + rank evals in parallel (≤4 GPUs) → epoch evals → results entry.
