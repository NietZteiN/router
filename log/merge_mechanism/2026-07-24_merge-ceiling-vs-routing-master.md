### Target Date: 2026-07-24 (Merge-ceiling vs routing master report)
- **Hypotheses / what we're testing:** Not a new training run — a **consolidation + verification** pass that assembles every merge result and the routing inventory into one report. Two standing claims re-tested at report scale:
  - **H-MASTER:** every *separable* weight-space merge plateaus at mu ≈ 0.41–0.48 ≈ base + dilution across all pools (P1–P5), all PEFT parameterizations, and all three task-vector types; only serve-time selection (routing / masks) escapes upward. CONFIRM = every exact separable merge cell within the band and every routing/mask cell above it; REFUTE = any *exact separable* merge clearing ~0.55.
  - **H-VERIFY:** the master-table numbers (RESULTS_TABLES / MERGE_METHODS_RESULTS) equal the on-disk per-run JSONs. REFUTE = any headline cell disagreeing with its JSON.
- **Setup:** Consolidated `../../tofu_sisa_lora/reports/MERGE_METHODS_RESULTS_2026-07-21.md`, `../../tofu_sisa_lora/reports/ROUTING_MASTER_2026-07-23.md`, and `../../merge-tables-7b/RESULTS_TABLES.md` into one thesis-first report: `../../tofu_sisa_lora/reports/MERGE_VS_ROUTING_MASTER_2026-07-24.md` (banded grand-master merge table + dilution law + PEFT/full-FT operator-independence + routing inventory + per-pool ceiling-vs-headroom + "selection is the carrier" + leak/exactness caveats). No GPU. Spot-verified headline cells by reading per-run JSONs directly (`metrics_version=ou-2026-06-10`, seed 42, smoke): 1B `checkpoints/Llama-3.2-1B-Instruct/results/smoke/merged_*.json`; 7B k=4 `checkpoints/Llama-2-7B-chat-hf_k4_r32_e5_lr1e4/results/smoke/*`; dilution per-k dirs `_k{4,10,20,50,100}_r32_e5_lr1e4` + `_k200_r8_e5_lr1e4`; P5 `_sift_masks` / `_clamu` / `_clamu_K200`; routing `_k200_r32_e25_lr1e4/results/smoke/routed_oracle_full.json`.
- **Results:**
  - **P1 (1B k=10)** live JSONs: `merged_additive_mean` 0.4190, `merged_additive_s0.05` 0.4285, `merged_dare_ties` 0.4236, `merged_della_ties` 0.4291, `merged_fisher` 0.4239, `merged_knots_ties` 0.4242, `merged_breadcrumbs_s0.0354` 0.4188, `subtract_orth` 0.4328, `merged_linear` 0.0497, `base_model` 0.3796 — all equal the master tables.
  - **P2 (7B k=4, `_k4_r32_e5_lr1e4`):** `merged_lorahub` 0.5921, `merged_dare_ties` 0.5445, `merged_jd_full` 0.5013, `merged_jd_diag` 0.5014, `merged_della_ties` 0.4969, `merged_knots_ties` 0.4894, `merged_fisher` 0.4765 — all match Table 2.
  - **Dilution law:** dare_ties 0.5445 (k4) → 0.4768 (k10) → 0.4504 (k20) → 0.4379 (k50) → 0.4299 (k100) → 0.4201 (k200 r8) — matches Table 3 cell-for-cell.
  - **P5 (1B full-FT):** `sift_full` 0.737, `merge_full` 0.4073, `sift_unlearn` 0.7377; ClAMU `merge_full`(Global) 0.3511, `emr_full` 0.3882, `tall_full` 0.4053, `clamu_full` K16 0.662 / K200 0.6716 — match Table 6.
  - **Routing:** `routed_oracle_full` (7B k=200 e25) 0.8236 (repo best) confirmed on disk.
  - **Data-hygiene finds:** `reports/all_metrics_smoke.csv` is a **stale pre-fix snapshot** (`merged_dare_ties` 0.1716 there vs live 0.4236); the bare `checkpoints/Llama-2-7B-chat-hf/` dir is a **legacy k=10 run** (dare_ties 0.4550), NOT the k=4 pool.
- **What worked / hypothesis verdict:** **H-MASTER SUPPORTED** at report scale — 19 separable operators land in 0.41–0.50; every break-downward row is 0.00–0.09; the only exact numbers above ~0.55 are serve-time selection (SIFT mask 0.737, ClAMU 0.672, routing 0.556–0.824). No exact separable merge cleared 0.55 ⇒ falsifier not triggered. **H-VERIFY SUPPORTED** for the live JSONs and both master reports; the one artifact that disagrees is the stale CSV (flagged, not cited).
- **Observations:** two gotchas that would have silently corrupted the tables, both caught only by direct-JSON spot-check: (1) the `reports/` CSV snapshot predates the `ou-2026-06-10` metric fix, so it under-reports every merge mu; (2) P2's k=4 numbers live in `_k4_r32_e5_lr1e4`, while the default-named `Llama-2-7B-chat-hf/` dir is an unrelated legacy k=10 run. Lesson reinforced: cite the versioned per-run JSONs, not the aggregate CSV, until the CSV is regenerated.
- **New questions / new hypotheses:** Does filling the queued **7B k=200 merge battery** (della/fisher/knots/tsv/slerp/lorahub/subtract_orth/JD — `../../merge-tables-7b/STATUS.md`) leave Table A's P3 row entirely in-band, or does any operator behave differently at true per-author granularity than at k≤10? (prediction: all in-band, per the N-ladder.)
- **Next Steps:** (1) regenerate `all_metrics_smoke.csv` via `collect_results.py --smoke` so the aggregate stops being a trap; (2) when the global 4-GPU cap has headroom, launch the pre-registered gapfill array (`eval_manifest_gapfill.txt`, `--merge_num_examples 32`) to populate the P3 merge cells — out of scope today (this was a CPU/reporting session).
