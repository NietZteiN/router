### Target Date: 2026-07-28 (APA uniform-summation study — instrumentation + four premise measurements)
- **Hypotheses / what we're testing:** New study (user-specified) of equal-weight parallel-LoRA aggregation, the "APA" rule `Δ = Σᵢ sᵢBᵢAᵢ` at coefficient 1.0 with no router. Three arms. **H-A1** utility degrades with the number N of adapters summed, and the degradation compounds rather than being flat. CONFIRM = monotone mu decline beyond the ±0.01 cross-campaign noise floor; REFUTE = flat within noise, as `additive_mean` already is. **H-A2 (matched-norm)** the sum-vs-mean difference is explained by delta MAGNITUDE alone: an arm at λ=1/√N (which holds ‖Δ‖ constant across N when the per-author deltas are orthogonal) tracks the mean arm, and all three arms collapse onto one curve in `rel_pert = ‖Σ‖_F/‖W₀‖_F`. **H-B1** an adapter trained on author X is selective for X — its removal costs X's questions much more than everyone else's (`S(X) = Δ_target/Δ_nontarget ≥ 3`). **H-B2** selectivity survives paraphrase: forget quality on X's paraphrased questions matches the original surface. **H-C1** a query no adapter owns is answered by a superposition of all N adapters (attribution `n_eff ≈ N`, not concentrated), and its utility cost therefore scales with the injected magnitude. Today is instrumentation + the premise measurements that gate the ladder; the ladder itself is queued.
- **Setup:** Llama-2-7B-chat-hf, per-author r32/α64/e5/lr1e-4 pool `/storage2/jack/checkpoints/tofu_sisa_lora/Llama-2-7B-chat-hf_k200_r32_e5_lr1e4/shard_{0..199}` (rslora, effective scaling α/√r = 11.3137). Config [`configs/nmerge_sum_expA_7b.json`](../../tofu_sisa_lora/configs/nmerge_sum_expA_7b.json): ladder N ∈ {1,2,5,10,20,32,64,128,200}, seed 42, probes pinned to {82,15,111}, arms λ=1 and λ=1/√N (the latter at N ∈ {2,10,20,64}), exact below N=32, `svd_rank` 1024 above. Eval convention unchanged from the e5 campaign (`--k 200 --forget_shard_id 199 --eval_shard_id <probe> --smoke`, `METRICS_VERSION ou-2026-06-10`). Code: `merge_subset.py` gains `--lam`/`--authors`/`--label`, `fixed_probe_authors`, `anchors.at_all_probes` and a `norms` subcommand; `analyze_nmerge.py` gains the `sum` label grammar + `mu_gmean`/`first_zero_component`; `eval_tofu.py` gains `per_example` sinks on `get_rouge`/`get_answer_probability`/`get_truth_ratio_scores` and `question_key` on the last (all default-identity); new `eval_mmlu.py`, `measure_expb_contrib.py`, `measure_adapter_selectivity.py`, `analyze_expa.py`, `submit_expa.sh`, `test_expa.py`. Gates green: `test_expa.py` (8), `test_merge_subset.py`, `test_ou_equivalence.py`, `test_eval_rows.py`, `test_merge_extra.py`. SLURM 449203 (merge smoke), 449218 (norms) — both PENDING behind other users at time of writing.
- **Results:**
  - **Delta geometry (`merge_subset.py norms`, seed 42, 3 slots + the full 224-slot pass).** Per-author effective deltas are near-orthogonal: mean |cos| **0.00085** (`layers.15.mlp.down_proj`), **0.00506** (`layers.0.self_attn.q_proj`), **0.00114** (`layers.31.mlp.up_proj`); max |cos| ≤ 0.0177. Consequently `‖Σ_N‖/‖Δ₁‖` = **1.42 / 2.26 / 3.19 / 4.54** at N = 2/5/10/20 against √N = 1.41 / 2.24 / 3.16 / 4.47. Full-slot ladder: `kappa = ‖Σ‖/√(Σ‖Δᵢ‖²)` = **1.0000 / 1.0007 / 1.0013** at N = 1/2/3. `rel_pert` (mean over slots) = 0.00465 / 0.00665 / 0.00817 at N = 1/2/3.
  - **Perturbed-split coverage (join on question text against `full`, 0 unjoinable).** `forget10_perturbed` → authors **180–199**, exactly **20 rows each**; `retain_perturbed` → authors **0–19**, 20 each. **Authors 20–179: zero rows.** `paraphrased_question` present and different from `question` on all 800 rows.
  - **`holdout10` overlap with `full`: 0 / 400.**
  - **Cached KS reference** `.../k200_r32_e5_lr1e4/results/smoke/retain_tr_scores.npy` is shape **(20,)**, mean **0.8318** — author 199 only.
  - **Existing on-disk comparators, re-read under one convention (probe 82, smoke).** `additive_mean`: mu 0.4596 / 0.4623 / 0.4613 / 0.4604 / 0.4589 / 0.4603 / 0.4598 / 0.4601 / **0.4593 (N=20)** / 0.4584 / 0.4598 / 0.4591 / 0.4597 (N=200), retain_ppl 7.70→7.89 — flat. `centered_pool` (≈ the unit sum, within ~6 % in norm): mu **0.4546 (N=2) / 0.4579 (4) / 0.4406 (8) / 0.4320 (20) / 0.4175 (32) / 0.3536 (64)**, retain_ppl 7.83 → 12.29 → 19.37. `sparse_dare0p9sum` (unit sum of DARE-sparsified deltas): mu 0.4543 / 0.4130 / **0.0750 (N=8)** / **0.0000 (N=16)**, retain_ppl 7.40 → 1415.67. Anchors: base 0.426, ft_r32 0.7563, retain90 0.5627.
  - **Firing baseline already on disk** (`reports/key_firing_e5.json` + its npz, this exact pool, 200 adapters × 203 groups): `gate_median` **1.1018**, verdict **LAZY**, `frac_ratio_lt_2` **1.0**. Mean ‖sBAh‖: on-author 0.1836, off-author 0.1665, world_facts 0.1645, real_authors 0.1650, alpaca 0.1600. Attribution on every OOD group: `max_share` **0.0053**, **n_eff 199.9 / 200**, `H_norm` **1.0000**.
  - **Regression:** re-running `analyze_nmerge.py` on the e5 campaign reproduces all 69 rows of `reports/nmerge_mu.csv` with **0 differing cells**; only `mu_gmean` / `first_zero_component` are added.
- **What worked / hypothesis verdict:**
  - **H-A1 pre-registered prediction REVISED before spending GPU.** The initial expectation (from `sparse_dare0p9sum` collapsing to mu 0 by N=16) was that the unit sum collapses by N≈20. That is **refuted for the dense sum**: with mean |cos| ≈ 0.001 the norm grows as **√N, not N** (4.54 vs √20 = 4.47 measured), so the DARE collapse is attributable to DARE's 1/(1−p) rescaling inflating each delta ≈3.2× (making `dare0p9sum` at N=8 ≈ 8.9 single-adapter norms), not to the sum rule. The `centered_pool` ladder — which I measured to be within ~6 % of the unit sum in norm — puts the real behaviour at a **graded** decline (0.455 → 0.432 at N=20 → 0.354 at N=64). The ladder was extended past the user's N=20 accordingly; stopping at 20 would have measured a plateau. INCONCLUSIVE pending the sum ladder itself.
  - **H-C1 SUPPORTED on the magnitude half, already, from data on disk.** Attribution over 200 adapters on unowned queries is `n_eff` **199.9/200**, `H_norm` **1.0000**, `max_share` 0.0053 ≈ 1/200 — maximally diffuse, with unowned queries firing every adapter at ~90 % of owner strength (0.160–0.165 vs 0.1836). There is no implicit routing to find. Still unmeasured: whether that survives into the served residual stream **signed** (base-h and unsigned in this measurement) — that is what `measure_expb_contrib.py` adds.
  - **H-B feasibility RESOLVED, and a documented invariant corrected.** `tofu_sisa_lora/CLAUDE.md` stated "TOFU's perturbed splits cover only ~2 rows/author" (a 400/200 population average) and that fq is NaN for single-author probes. Measured: coverage is **all-or-nothing** — 20 rows for authors 0–19 and 180–199, none for 20–179. So per-author Forget Quality is perfectly well defined (n=20) for the covered authors, which explains why `iso_a15` has fq 0.1745 while `iso_a{82,111,177,76}` are NaN. Combined with the retain90 oracle training on 0–179, **only authors 180–199 are legal Exp-B targets**. Both invariants corrected in CLAUDE.md in the same change.
  - **Two silent-corruption traps found and closed.** (i) `analyze_nmerge._LABEL_RE` matched `add|dare|cpool|cr\d+` only, so every `nmerge_sum_*` row parsed to `kind="other"` and was dropped at line 163 **with no error** — and patching the regex without the tag map yields `centered_lowrank_rm` (`"sum"[2:]`), a wrong-but-plausible method label. Both patched together and pinned by an explicit negative assertion. (ii) The cached KS reference is author-199-only and `--eval_shard_id` never re-derives it, so reusing it for another author would KS two different question sets; `measure_adapter_selectivity.py` now verifies a recorded row-set hash and refuses a mismatch.
- **Observations:** (i) `model_utility` is a **step function** near collapse — scipy `hmean` returns exactly 0 if any one of its 9 components is 0 (observed at `sparse_dare0p9sum_N16`: mu 0.0 purely because `real_rouge` = `world_rouge` = 0, while `retain_truth_scaled` 0.151 / `real_prob` 0.2366 / `world_truth_scaled` 0.309 still carried signal), and 2 of the 9 are saturated across the whole mean ladder (`real_rouge` 0.982–0.992, `world_rouge` 0.90–0.94). `mu_gmean` + `first_zero_component` added for that reason; MMLU added as an unsaturated channel with a hard 0.25 floor, with letter-entropy recorded because argmax-over-4 cannot distinguish "degraded" from "degenerate constant-letter". (ii) The cross-convention noise floor is ~0.01 mu (same base model, same metrics version: base_model 0.4179 at the k=10 convention vs 0.4260 at k=200/probe-82, the entire gap coming from the retain triple) — the whole "0.42–0.46 merge band" is only 0.04 wide, so only same-pool/same-convention comparators are admissible. (iii) Disk is the binding constraint: `/storage2` at 98 %, 143 GiB free; this config's 12 merges cost 55.0 GiB (2.013 M × rank × 4 B; 7.68 GiB per rank-1024 merge), which is why seeds 43/44 are staged separately and the λ arm carries a per-λ ladder restriction. (iv) Silent-failure checks clean so far: no NaN in the norms ladder, `kappa` within 0.0013 of 1, decomposition exactness asserted in-job.
- **New questions / new hypotheses:** (1) Does unowned-query degradation collapse onto a single curve in `rel_pert` across all three arms? If yes, uniform summation costs utility purely by injecting magnitude and the 1/N is nothing but norm control. (2) Is the cancellation index ‖Σcᵢ‖/Σ‖cᵢ‖ ≈ 1/√n (mutually orthogonal, noise grows √N) or ≈1 (reinforcing)? (3) Given `gate_median` 1.10 on base hidden states, the arithmetic prediction at N=20 is `foreign_mass(X) ≈ 19/(19+1.10) ≈ 0.945`, i.e. dropping X removes only ~5 % of the aggregate's response to X's own questions ⇒ **S(X) ≈ 1, no selectivity**. Falsifier: S(X) ≥ 3 with non-target retention ρ ≥ 0.95. (4) Does the paraphrase axis even have headroom — is `m_para(iso_X) ≫ m_para(base)`? If not, any orig-vs-para gap is an adapter-generalization result, not an unlearning one. (5) e5's per-author ROUGE headroom is only ~0.11 (iso 0.402–0.575 vs base floor 0.404) though `forget_ppl` separates 4× (3.5–3.9 vs 14.6); Exp B should run on the e25 pool, pending a completeness check of its 200 shards.
- **Next Steps:** merge array 449203 (12 tasks, CPU) → `norms` 449218 → eval array `%4` → `mmlu` array `%4` chained `afterany` (never two GPU arrays queued at once; `kill_invalid_depend` is off cluster-wide) → `contrib` on the uncompressed merges only → `analyze_expa.py` → figures under base python. Then Exp B on e25 with targets from {186, 189, 191, 194, 196}, per-(author,surface) KS references built from the retain90 oracle first. Cluster was saturated (26 running / 17 pending, all other users) at time of writing; both jobs queued at Priority.
