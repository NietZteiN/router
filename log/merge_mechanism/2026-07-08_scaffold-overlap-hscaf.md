### Target Date: 2026-07-08 (H-scaf: does a scaffold absorb the shared output component?)
- **Hypotheses / what we're testing:** H-scaf (opened 2026-07-07, per-author-similarity entry): experts trained on a *scaffolded* base show materially lower col(B) overlap and shared-subspace energy than plain shards — the scaffold absorbs the common "QA-style" output component, leaving more idiosyncratic private deltas. CONFIRM: null-calibrated col(B) ratio and energy/chance clearly below plain comparators. REFUTE: ratios equal or higher. **Design constraint discovered at setup:** no plain Llama-3.2-1B k10 r32/e5 collection exists (the plain 1B k10 is legacy r8/α16), so the test is a *triangulation*: (i) same-recipe cross-model comparator — plain Llama-2-7B k10 r32/e5 (new run); (ii) same-model cross-recipe comparator — the existing plain 1B k10 r8 numbers (`subspace_overlap_k10.json`). Each comparison is confounded (model size / recipe respectively); only both pointing the same way is evidence.
- **Setup:** `subspace_overlap.py` (unchanged from yesterday, sha `1120d2de4d34…`), `--rank 16 --n_null 20 --seed 42`, 16 CPU threads, srun on sprint nodes: (A) scaffolded experts `/storage2/jack/checkpoints/tofu_sisa_lora/Llama-3.2-1B-Instruct_experts_scaf_k10/shard_{0..9}` (r32/α64/e5 rslora on the Alpaca-scaffolded base) → `reports/subspace_overlap_scaf_k10.{json,csv}` (SLURM 441240); (B) plain `Llama-2-7B-chat-hf_k10_r32_e5_lr1e4/shard_{0..9}` → `reports/subspace_overlap_k10_r32_7b.{json,csv}` (job id not captured — srun allocated immediately, no queue line; commands + seed above are the reproducibility anchor). First launch attempt failed silently-ish (shell cwd reset → empty adapter glob + script-not-found, exit 2); rerun used absolute paths + an explicit 10-shard count guard.
- **Results:** (each vs its OWN orthogonal null; null col(B) scales ≈√(r/d) so raw values are not cross-model comparable)
  | collection | cos mean (z) | col(B) real / null → ratio | row(A) real / null | energy@r16 / chance |
  |---|---|---|---|---|
  | scaf 1B k10 r32 | 0.00425 (z=901) | 0.2364 / 0.1326 → **1.78×** (excess +0.104) | 0.0973 / 0.0969 | 0.2987 / 0.05 → **5.97×** |
  | plain 7B k10 r32 | 0.00696 (z=4280) | 0.1558 / 0.0699 → **2.23×** (excess +0.086) | 0.0711 / 0.0699 | 0.3916 / 0.05 → **7.83×** |
  | plain 1B k10 r8 (2026-06-29) | 0.0212 (z=3657) | 0.2617 / 0.0653 → **4.01×** (excess +0.196) | 0.0554 / 0.0479 | 0.6497 / 0.20 → 3.25× |
- **What worked / hypothesis verdict:** **H-scaf DIRECTIONALLY SUPPORTED, not yet clean.** Both available comparisons point the same way: vs the same-recipe plain 7B, the scaffolded experts have lower calibrated col(B) ratio (1.78× vs 2.23×), lower shared energy (5.97× vs 7.83× chance), and ~40% lower pairwise cosine (0.0043 vs 0.0070; z 901 vs 4280). Vs the same-model legacy plain 1B, the col(B) excess halves (+0.104 vs +0.196) — though the 7B row shows a good chunk of that drop is the r8→r32 recipe, not the scaffold. Crucially the effect is **partial**: the scaffolded experts still carry a large, unambiguous shared output component (col(B) z ≈ 3.8e4 vs its null sd). The scaffold absorbs *some*, not most, of the common component. Clean CONFIRM/REFUTE still requires the missing control.
- **Observations:** (a) Raw col(B) comparisons across models are a trap — the scaf collection's raw 0.2364 *exceeds* plain 7B's 0.1558 purely because the 1B null floor is higher (d=2048 vs 4096); only null-calibrated ratios are meaningful. (b) row(A) at its null replicates on the scaffolded base and at 7B r32 — input-side randomness is now confirmed across 3 bases/recipes + k=200. (c) Linkage to Exp-5's sign-flip (ρ=−0.675, col(B) overlap = *survival* under averaging): if scaffolding makes private experts more idiosyncratic, they should dilute away *faster* under merged serving while routing is unaffected — scaffold-maximization and merge-serving pull in opposite directions; for the routed/O(1)-deletion design this is fine, arguably desirable (less cross-author leakage in the weights). (d) Ops: the cwd-reset failure produced exit-2 srun tasks whose absence of output JSONs was the only signal — the count guard + absolute paths are now the pattern.
- **New questions / new hypotheses:**
  - **H-scaf-control (open):** train the missing plain Llama-3.2-1B k10 r32/α64/e5 shards (10 cheap 1B GPU jobs, ~400 samples each) and rerun — the decisive same-model same-recipe contrast. Prediction: calibrated ratios land between scaf (1.78×) and legacy 1B (4.01×), clearly above scaf.
  - **H-scaf-dose (open):** a domain-schema scaffold (synthetic same-format biographies, not generic Alpaca) absorbs more of the common component than alpaca2k — the "maximize S" dial from the shared-scaffold design discussion.
  - H-rank (r8/r1 k200 capacity dial) still open, unchanged.
- **Next Steps:** (1) submit the 10-shard plain-1B r32/e5 control training + overlap rerun; (2) if control confirms, fold H-scaf into the shared-scaffold/minimal-private design memo (scaffold absorbs part of S; disjoint-slice or frozen-basis handles the rest); (3) H-rank runs remain queued behind it.
