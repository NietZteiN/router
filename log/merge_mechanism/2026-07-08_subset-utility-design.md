### Target Date: 2026-07-08 (subset-conditioned utility, dense N=1–20 — design + pre-registration)
- **Hypotheses / what we're testing:** Follow-up to Exp-5 ([results](2026-07-08_interference-vs-n-results.md)) addressing a real measurement objection: the standard `model_utility` retain split samples ALL 200 authors, so for a partial merge only ~N/200 of retain rows belong to merged authors — mu cannot answer *"did the model learn the subset it was trained on."* Fix: new `eval_tofu.py --retain_author_ids` restricts the retain_* metrics (prob/rouge/ppl + retain truth-ratio) to the merged authors' rows. Dense ladder in the region of interest: N ∈ {1, 2, 3, 4, 6, 8, 10, 12, 16, 20} (+ 32/64/128/200 context).
  - **H4 (knee on learned material):** subset-conditioned retain utility decays monotonically with N with its knee at N≈4–8, mirroring the probe-recall collapse. CONFIRM: `subset_retain_prob` falls from its N=1 value toward the full-population level (≈0.234) with ≥50% of the total fall already realized by N=8. REFUTE: flat through N=20, or knee well past 8.
  - **H5 (isolation cost precedes merging):** even at N=1, the isolated adapter's subset-conditioned retain utility is far below the joint-ft ceiling on the same rows. CONFIRM: iso `subset_retain_prob` ≤ 0.6 × ft-on-subset(8). REFUTE: iso ≈ ft (then all damage is merge-caused). (The Exp-5 ROUGE analogue was iso 0.51 vs ft 0.91.)
- **Setup:** Same pipeline/conventions as Exp-5 (config `configs/nmerge_interference_7b.json`, seed-42 nested subsets, smoke caps, forget author 199, materialized CPU merges). New: (a) `--retain_author_ids` in `eval_tofu.py`/`eval_baseline.py` (default = unchanged legacy split, `test_ou_equivalence.py` green); (b) eval manifest gains a 4th column `retain_ids` → result files `{label}__subset.json`; (c) ladder densified — new exact merges N ∈ {3, 6, 10, 12, 20} + rebuild N16/N32 (safetensors deleted in yesterday's cleanup; deterministic rebuild); (d) subset-utility rows at every ladder N with retain restricted to subset(N) = perm42[:N]; anchors `ft_r32_sub8` / `base_model_sub8` = joint-ft ceiling and base floor restricted to subset(8)'s rows; (e) denser recall probes at N ∈ {3, 6, 10, 12, 20}. ≈39 new smoke evals + 7 CPU merges. Caveat pre-declared: TOFU's perturbed splits cover ~2 rows/author, so the subset-restricted retain truth-ratio may be empty at small N → `model_utility` NaN there; H4/H5 are judged on `retain_prob` (primary), `retain_rouge`, `retain_ppl`.
- **Results:** [pending]
- **What worked / hypothesis verdict:** [pending]
- **Observations:** [pending]
- **New questions / new hypotheses:** [pending]
- **Next Steps:** merges → micro-smoke one subset eval (check the restricted-pool row count in the log: N=4 → 80 rows) → full wave → `analyze_nmerge.py` subset CSV → fig5 (subset utility vs N) + denser fig2 → results entry.
