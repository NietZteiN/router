### Target Date: 2026-07-14 (scaffold and metric port)
- **Hypotheses / what we're testing:** Setup day — no training run yet. The thread's standing hypotheses (H1–H5, see [README](README.md)) were registered before any GPU job. Today's falsifiable sub-claims: (a) the paper's four aggregate metrics (Util.R/Util.G/Mem./Priv.) can be composed from stock OpenUnlearning `TOFU_EVAL.json` outputs precisely enough to reproduce Table 1's Finetuned row (1.00/1.14/0.07/0.38) and Retrained row (1.00/1.11/0.58/1.00) from the *public* checkpoints — CONFIRM: composed values within ±0.02 (Priv/Util) of the paper; REFUTE: any axis off by > 0.05. (b) Our in-house TOFU data pipeline is token-for-token identical to OpenUnlearning's — CONFIRM: direct import-and-compare equality on real rows.
- **Setup:** New project [`memadapt_tofu/`](../../memadapt_tofu/) @ git `0066f1a`, seed 42. open-unlearning @ `4ad738a` (main) + additive branch `memadapt-eval` @ `7cea9fe` (registry shim for `MemAdaptLlamaForCausalLM`, `configs/model/MemAdapt-Llama-3.2-1B.yaml`, `configs/eval/tofu_grimes.yaml` = default metrics + exact_memorization + mia_{loss,zlib,min_k,min_k++}). Canonical eval logs from HF `open-unlearning/eval` → `/storage2/jack/memadapt/eval_refs/{full,retain90}_eval.json`; `open-unlearning/tofu_Llama-3.2-1B-Instruct_retain90` (snapshot `7114300`) + `open-unlearning/idk` downloaded to HF_HOME. CPU tests: `cd ~/memadapt_tofu && ~/anaconda3/envs/test-env/bin/python -m pytest tests/ -q` (test-env: torch 2.5.1+cu121, transformers 4.48.3). Metric self-check: `python eval_compose.py --self_check`. No SLURM jobs today (login-node CPU only); `unlearning` conda env (py3.11, OU pinned deps) building in background.
- **Results:** CPU suite **24/24 passed** (product-key top-k == brute force incl. tie case; zero-init adapter bitwise == base model on a tiny Llama; gradient isolation: non-owned rows exactly 0, owned rows == per-source-restricted unmasked grad; block-list exclusion + candidate-grid renormalization; block-everything ≡ 0 (NaN guard); per-row block-lists; checkpoint round-trip bitwise + sha tamper rejected; OU tokenization/collator parity on rows {0,1,19,20,1999,3999}; forget10 ↔ 20 whole authors via exact text join). Metric composition self-check: Priv(Finetuned) = **0.3810** (paper 0.38), Priv(Retrained) = **1.0000** (by construction), Mem(Finetuned) = **0.0871** (paper 0.07), Mem(Retrained) = **0.5906** (paper 0.58), Util.R(Retrained) = **1.0125** (paper 1.00); implied base Util.G denominator **0.4927** (from Finetuned) vs **0.4941** (from Retrained).
- **What worked / hypothesis verdict:** Sub-claim (a) **SUPPORTED**: Priv exact to 3 decimals against the paper on both anchors; Mem within +0.017/+0.011 — consistent with Grimes training their own finetuned/retrained models (their Table 2 runtimes prove they did) rather than a composition error; the two independent Util.G denominator estimates agree to 0.003 (strong internal consistency). Sub-claim (b) **SUPPORTED**: token-for-token equality vs OU's actual `preprocess_chat_instance`/collator (imported directly, not re-derived). H1's CPU half passed (bitwise no-op at zero init); GPU half pending.
- **Observations:** Two instructive test-reference bugs (both mine, not the implementation's): (1) under a block-list, product-key retrieval is *inexact* vs full-table top-k — blocked pairs crowd the per-half shortlists (93.75% selection agreement at toy scale); the deployed/paper semantics block on the candidate grid, so the reference must too. (2) The invariant "masked grad == unmasked grad restricted to owned rows" is subtly wrong: cross-source reads contribute to the unmasked grad; the correct invariant is per-owner-restricted. Both now encoded in the tests. Engineering: only the 51,200 assigned rows are materialized (compact table + pad row + remap, `embedding_bag(padding_idx)`) — bit-exact vs dense, ~1.7 GB total optimizer footprint; fp32 routing everywhere with autocast force-disabled inside the module (HF Trainer bf16 autocast would otherwise change top-k selections vs profiling/eval); weight_decay pinned to 0 (OU's 0.01 would decay idle sources' entries ~17% over 15 epochs).
- **New questions / new hypotheses:** Is `hydra` CLI group override `eval=tofu_grimes` compatible with the experiment file's `override /eval: tofu` (verify in the S2 eval-plumbing smoke)? Where does the frozen-router routing health land (own-entry hit rate, effective reads) — the S4 gate decides whether key-init rescaling is needed before the 15-epoch spend? Does the greedy TF-IDF assignment fill all 200×256 slots without fallback (fallback fills = routing-collapse alarm)?
- **Next Steps:** (1) Adversarial code review (in flight) → fix confirmed findings. (2) Finish `unlearning` env; verify `MemAdaptLlamaForCausalLM` CPU-loads under transformers 4.51.3. (3) Submit S2 GPU smoke chained `afterany` behind the pending pfx-bake arrays (global 4-GPU cap is saturated); confirm A40 + VRAM headroom. (4) S3 calibration evals (retain90/full/base) → gate G1. (5) S4 assignment + routing-health gate → S2b LR pilot → S5 15-epoch train.
