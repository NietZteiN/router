### Target Date: 2026-07-14 (review fixes, smoke submitted)
- **Hypotheses / what we're testing:** Follow-up to [scaffold-and-metric-port](2026-07-14_scaffold-and-metric-port.md). Sub-claim: an adversarial review with library-source verification finds integration bugs the 24-green CPU suite cannot see (cross-env config composition, dead parameters) — CONFIRM: ≥1 confirmed bug with a concrete wrong-numbers scenario; REFUTE: review returns only style nits. Also submitting S2 (GPU half of H1 + VRAM/A40 confirmation).
- **Setup:** Review agent over `memadapt_tofu` @ `0066f1a` + OU branch `memadapt-eval` @ `7cea9fe`, with instructions to verify against installed library code (transformers 4.48.3 trainer source, torch 2.5.1 autocast lists, hydra `--cfg job` composition in the `unlearning` env). Fixes at `fdb5d16`; CPU suite + `eval_compose.py --self_check` re-run post-fix. Smoke: `./submit_memadapt.sh smoke` → **SLURM job 443276** (queue empty at submission — pfx-bake arrays drained; cap usage 1/4), config `configs/memadapt_tofu_1b.json`, seed 42.
- **Results:** **2 confirmed bugs.** (1) *Critical:* OU's eval experiment file (`configs/experiment/eval/tofu/default.yaml`, `@package _global_`) merges after the `model=` group choice and resets `pretrained_model_name_or_path` to `open-unlearning/tofu_Llama-3.2-1B-Instruct_full` — verified by composing the exact eval command with `--cfg job`: the MemAdapt adapter (profiled/trained against the pristine instruct model) would be installed on the TOFU-finetuned base with no crash and plausible generations; every S6 number would have been silently invalid. (2) `hard_zero` model_arg accepted but never read (dead code → mislabeled ablation arm). Also flagged: grad-isolation assert unsound under gradient accumulation (latent S2b trap); `overwrite: false` stale-eval-cache trap; "static routing" claim overstated (bf16 ulps can flip near-tie top-k); `eval_compose` 0/0 AUC edge. **Verified correct against sources:** Trainer 4.48.3 signatures + `num_items_in_batch` flow; embedding_bag absent from torch autocast cast lists (fp32 path safe); OU `get_model` passes kwargs by name, never calls `.to()`/`accelerator.prepare`; `eval=tofu_grimes` CLI group override composes all 12 metrics; checkpoint saved on torch 2.5.1 loads bitwise on 2.4.1; greedy assignment at realistic 13M-pair scale = 4.5 s; TOFU full/forget10 contain zero duplicate (q,a) pairs (join is collision-free). Post-fix: **24/24 CPU tests, self-check PASSED** (Priv FT 0.3810 unchanged).
- **What worked / hypothesis verdict:** Sub-claim **SUPPORTED** — the review's headline find (eval base-model clobber) is exactly the class of silent cross-env bug the ±0.03 acceptance budget cannot survive, and it was caught before any GPU spend. The fix is one explicit CLI override, now marked load-bearing in `submit_memadapt.sh` and trap #5 in the project CLAUDE.md.
- **Observations:** Hydra `@package _global_` experiment files overriding *after* group selection is a foot-gun for every future OU-integrated project here — worth remembering beyond this thread. The reviewer measured the greedy assignment loop at 4.5 s (no need for the vectorized rewrite contingency).
- **New questions / new hypotheses:** Does the S2 smoke confirm A40/46GB and the ~10 GiB typical VRAM estimate? Does the 2-author full-size-table smoke assignment produce sensible own-coverage numbers (preview of the S4 gate)?
- **Next Steps:** On smoke PASS: submit S3 calib array (`./submit_memadapt.sh calib`, %2) and S4 assignment (1 GPU) in parallel (2+1 = 3 ≤ 4 with smoke done); then S2b LR pilot; then S5. On smoke FAIL: fix, new entry.
