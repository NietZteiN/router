### Target Date: 2026-07-16 (Wave-0 build — all four arms coded + CPU-gated green)
- **Hypotheses / what we're testing:** No new hypotheses — this entry records the Wave-0 BUILD
  for the pre-registered thread ([thread-preregistration](2026-07-16_thread-preregistration.md)).
  Build-level claims verified today: (B1) the linearized forward equals the closed form
  f(θ0)+J·τ and superposes exactly; (B2) the write-disjoint projection keeps col(B_a) ⊆ span(Q_a)
  through real optimizer steps; (B3) disjoint-support merged weights restricted to S_a equal τ_a
  and subtract ≡ recompose-without BITWISE; (B4) the additive_sum merge path minus τ_u equals the
  remerge at algebraic class (rel ~7e-8); (B5) the diff attack separates a planted leaky pre/post
  pair (AUC 1.0) and reads clean pairs at ~0.5.
- **Setup:** Built by 6 concurrent implementation agents + 1 integration pass + a do_verify/chain
  fix (single-writer). All CPU, no SLURM jobs, no GPU. Interpreter test-env python. New files
  (sha256, /home/jack/tofu_sisa_lora/): linear_tv.py 346ffa97… · train_linear_tv.py f596d52c… ·
  test_linear_tv.py 49f543b8… · struct_bases.py 12fede81… · train_struct_tv.py 93a7349c… ·
  verify_struct.py fcc7caa7… · test_struct_tv.py c6c76a2f… · ds_support.py c4036eb7… ·
  train_ds_support.py (see meta) · test_ds_support.py 01cbfe77… · sparsify_pool.py +
  test_sparsify_pool.py · verify_subtraction.py + test · attack_diff.py + attack_mia.py
  --dump_scores · merge_subset.py additive_sum (additive-only; full pre-existing suite kept
  green — Exp-6 in flight untouched) · submit_ctv.sh 946bde53… (superseded by the do_verify fix,
  final sha in Wave-1 entry) · analyze_ctv.py + test_analyze_ctv.py 746ef787… · eval_tofu.py
  09413efa… (adds --linear_tv_* and --ds_* serve bypasses + a loud conflict guard over the 8 arm
  selectors; flag-absent path unchanged) · configs/ctv_1b_{ctrl,lin,wd,ds}.json +
  ctv_1b_lin_nlserve.json + sparsify_7b.json. tofu_sisa_lora/CLAUDE.md updated (same-change rule).
- **Results:** Full gate suite GREEN: test_linear_tv (14 checks incl. closed-form linearity vs
  autograd, exact superposition, subtract≡recompose ≤1e-6, A.grad=0 at B=0, byte-determinism,
  B-only round-trip) · test_struct_tv (QR bases orthonormal + cross-author orthogonal +
  deterministic; projection idempotent; callback holds constraint through 3 real Adam steps;
  merge-drop ≡ Q_a-zeroing; planted-empty + unprojected pools FAIL the right checks) ·
  test_ds_support (14 tests incl. bake bitwise subtract≡without; in-place eval serve ≡ bake
  bitwise; locality CLI catches a planted off-support index) · test_sparsify_pool (DX1 math on
  known-cancellation fixtures; DX2 ≈1/N on synthetic) · test_verify_subtraction (class ladder:
  declaration holds iff measurement at least as strong) · test_merge_subset FULL (pre-existing +
  additive_sum case) · test_deletion_audit FULL (dump→diff e2e: leaky 1.000/clean 0.5) ·
  test_analyze_ctv (manifests, STUB previews all 5 configs). bash -n green; STUB=1 `all`
  previews for ctrl/wd/lin/ds/lin_nlserve all rc=0 with correct commands (--arm dispatch, lin
  rows via --linear_tv_config with NO --preloaded_adapter, ds rows via --ds_config, merges via
  derived merge_cfg_*.json into mtmp_<variant>/ with absolute symlinks to ctv labels).
- **What worked / hypothesis verdict:** B1–B5 all SUPPORTED by the gate suite (evidence above).
  Integration drift between concurrently-built components was real and caught pre-submission:
  --variant vs --arm, dict-form scale_conditions crashing prep, lin eval rows silently serving
  NONLINEAR via --preloaded_adapter precedence (now a hard ValueError), merge_subset.load_config
  key requirements, manifest filename collisions between configs sharing an out_dir, and the
  `all` chain racing evals ahead of trains for merge-less arms.
- **Observations:** (1) Storage decision: [ds] ladder/iso conditions serve IN-PLACE via a new
  eval_tofu --ds_config bypass (sparse τ applied to θ0 at load; zero-init accumulator keeps
  subtract bitwise) instead of materializing full checkpoints — 20 iso bakes would have cost
  ~100 GB against 170 GB free; bake_merged is retained only for the G4 headline bake. (2) W3
  (sign-fixed LoRA) killed at design per pre-registration; DX1 closes it with data for free.
  (3) do_verify redesigned: verify_subtraction is a POST-merge (G3) certificate, not a
  post-train gate; per-arm post-train gates = verify_struct (wd gating, ctrl report-only) and
  ds locality; lin skips (no structural invariant). (4) GPU-phase risks recorded for Wave-1:
  lin eval has no KV cache (O(T²) generation, batch-1) — 01:30:00 may be tight at smoke caps;
  jvp needs eager attention; gradient checkpointing incompatible with functional_call;
  structural arms save fp32 adapters (bf16 would break the ≥1−1e-6 own-energy certificate).
- **New questions / new hypotheses:** (a) Is the ctrl pool (unconstrained frozen recipe) the
  right H-lin-1 twin, or does the lin arm need a frozen-A NONLINEAR control (train_lora_shard
  --irp_seed) to unconfound linearization from the frozen-A restriction? — under adversarial
  review now, resolve before G1 verdicts. (b) Does the lin smoke eval fit 01:30:00 on an A40?
- **Next Steps:** Adversarial pre-GPU review verdict → fix any CRITICAL/MAJOR findings →
  Wave-1: real `gate` stamps + `prep` per arm, train arrays at ARRAY_CAP=1 (queue holds
  444061_[0-124%3] = worst-case 3 GPUs; 3+1 = the global cap of 4), w5_build CPU job (sparsify
  grid + DX1/DX2), G1 iso+base evals via EVAL_ARRAY row-range (ladder evals held until G1
  verdicts per the kill-gate discipline).
