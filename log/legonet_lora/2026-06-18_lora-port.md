### Target Date: 2026-06-18 (LegoNet→LoRA port for exact record-level unlearning)
- **Goal / Hypothesis:** Implement + test a faithful LegoNet→LoRA→LLM port for *exact, verifiable*
  record-level unlearning (new project `~/legonet_lora`): frozen base + n LoRA adapters with FROZEN
  k-means keys (MiniLM emb), semantic top-k k-NN routing; deleting a record retrains only the k
  activated adapters. H1: semantic routing produces meaningful, balanced clusters on a real corpus
  (where TOFU collapsed). H2: post-unlearn adapters reproduce a from-scratch retrain (exactness),
  ideally bitwise.
- **Setup:** Plan `~/.claude/plans/papers-legonet-merging-loras-like-partitioned-russell.md` (approved).
  Decisions: base=Llama-2-7B-chat-hf, encoder=all-MiniLM-L6-v2 (installed sentence-transformers 3.4.1
  `--no-deps`; transformers 4.48.3/peft 0.14.0/torch 2.5.1 untouched), corpus=DBpedia-14 + per-record
  Secret-Sharer canary (keys from disjoint test split = airtight external reference). Configs
  `configs/legonet_{7b,smoke}.json`. Scripts (sha256 first16): legonet_common 728c3e1d, build_corpus
  39a91d15, keys 87ffd22c, routing e262edb4, train_adapter 9741b396, combine 691337e4, unlearn
  625d3a49, eval_memorization e3f1d6ab (canary_em added post-smoke), verify_exactness c3604494,
  run_exactness_sample f166655f, submit_legonet 46bfa0a0. Corpus (CPU): dbpedia_n4000 balanced
  285–286/class × 14; reference 4000 from test split. Keys n=32 (inertia 2988). Assignment n32/k3.
  CPU tests `python tests/test_{routing,metrics,exactness,pipeline}.py`. SLURM (sprint1-3, %12, 64G):
  smoke 435392→435393[0-3]→435394∥435395; 7B 435568→435569[0-31%12]→435570∥435571.
- **Results:**
  - **CPU tests ALL GREEN.** Mechanism validated on a tiny random Llama: reproducibility bitwise
    (max_abs 0), untouched-adapter invariance bitwise, affected adapter changes (rel_l2 1.8e-2),
    disabled→zero-delta; full pipeline deletion exactness bitwise (max_rel_l2 0, structural_ok).
  - **Routing genuinely semantic (H1 ✓):** DBpedia n=32/k=3 adapter sizes 83–664 (mean 375 = kN/n),
    **0 empty**, mean cluster purity **0.63** vs 14 classes (≠ TOFU centroid collapse). Imbalance ~8×
    (load-balance failure mode noted; balanced-kmeans mitigation deferred).
  - **Phase-0b smoke (TinyLlama, synthetic) DONE.** 4 adapters (counts 35/26/20/39). Eval: legonet
    EM 0.529 / VerbMem 0.463 vs base EM 0.244 / VerbMem 0.019. **Exactness on GPU: all 5 deletions
    bitwise=True, max_rel_l2=0.0, structural_ok** — bitwise holds on GPU for TinyLlama (stronger than
    the distributional fallback the plan hedged for; H2 ✓ at smoke scale).
  - **7B chain (435568–571) QUEUED** at end of session.
- **Observations:** Smoke exposed that free-generation `canary_hit` is uninformative (0 even when
  trained — greedy gen must reproduce the whole abstract before reaching the code). Fixed by adding a
  **teacher-forced `canary_em`** (predict canary tokens given the TRUE preceding context) as the primary
  clean forget signal; `canary_hit` kept as stringent secondary. CPU tests re-run green. Smoke-before-7B
  discipline paid off — caught the metric flaw before 7B compute.
- **Next Steps:** Read 7B results `runs/legonet_7b_n32_k3/results/{eval_legonet,eval_base,exactness}.json`
  + `reports/exactness_table.md`. Expect legonet canary_em ≫ base; post-unlearn canary_em → base on
  deleted records; affected adapters bitwise/within-tol vs oracle. Phase 3: n∈{16,32,64}/k∈{1,2,3,5}
  sweep + SISA-LoRA baseline (s=n/k), measure k²N/n vs N/s crossover + utility. Consider balanced
  k-means if fat clusters slow retrains.

