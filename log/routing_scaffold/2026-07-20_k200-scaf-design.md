### Target Date: 2026-07-20 (H-k200-scaf pre-registration — scaffolded per-author experts + oracle routing)
- **Hypotheses / what we're testing:** Can a public scaffold push the new 0.8236 headline
  ([2026-07-20_k200-oracle-routing-results.md](2026-07-20_k200-oracle-routing-results.md))
  further? The 0.8236 run's mu is capped by the PLAIN base's OOD components (real_prob
  0.778 / world_prob 0.679 enter the harmonic mean; retain is saturated at 0.999/1.000).
  This campaign rebuilds the k=10 scaffold recipe at k=200/7B: bake an Alpaca-2k scaffold
  into the base, train the 200 per-author e25 experts ON the scaffolded base, serve
  oracle-routed (author → base+scaffold+expert; OOD → base+scaffold).
  - **H-scaf-k200-1 (floor mediator):** the scaffolded 7B base preserves or raises the OOD
    components vs the plain base. CONFIRM: real_prob ≥ 0.758 and world_prob ≥ 0.659
    (within −0.02) on the `scaffolded_base_floor` row, ideally above 0.778/0.679.
    REFUTE: either drops > 0.02 (Alpaca SFT damages 7B world knowledge → premise dies).
    1B precedent: the scaffold *preserved* the base floor (0.63/0.66), didn't lift it.
  - **H-scaf-k200-2 (headline):** oracle-routed scaffolded experts beat the plain-base
    headline. CONFIRM: mu ≥ 0.83. REFUTE: mu ≤ 0.8236. Honest prior: UNCERTAIN — at
    k=10/1B the scaffold's +0.19 came almost entirely through stronger experts
    (retain_prob 0.304→0.854), a channel already saturated here (0.999); the only
    remaining room is the OOD components, so H2 rides on H1 producing a positive lift,
    which the 1B evidence does not promise. A clean REFUTE would itself be publishable
    scope: "the scaffold's value = expert quality, not OOD competence."
  - **H-scaf-k200-3 (deletion):** deletion stays exactly utility-free on the scaffolded
    stack: |Δmu| ≤ 0.005; deleted author falls to the scaffolded-base floor (f_ppl ≈ the
    floor row's forget_ppl, f_rouge ≈ its forget_rouge).
- **Setup:** driver `submit_k200_scaf.sh` (sha256 d02e4524a04a40d0), STUB-previewed; smoke
  tier, seed 42, `--k 200 --forget_shard_id 199`, `--lazy_adapter_cache 8`.
  Chain (each stage `--dependency=afterany`, self-skipping):
  1. `train_scaffold.py` 7B + Alpaca-2k (defaults r16/α32/e3/lr2e-4 non-rslora) →
     `Llama-2-7B-chat-hf_scaffold_alpaca2k`;
  2. `make_scaffolded_base.py` → `Llama-2-7B-chat-hf_scaffolded_alpaca2k` (~13 GB);
  3. 200 × `train_lora_shard.py --model_name <scaffolded base> --epochs 25 --k 200`
     (frozen recipe) → `Llama-2-7B-chat-hf_k200_r32_e25_scaf_lr1e4/shard_*` (~50 GB);
  4. evals: `routed_oracle_full.json`, `routed_oracle_del199.json`
     (`eval_routed_scaffold.py`), `scaffolded_base_floor.json` (`eval_baseline.py`,
     the H1 readout).
  ⚠ Cap math at submit time (amended minutes after submission, before any task ran): the
  co-queue plan (their %1 + our %3 = 4) was invalidated mid-submission — the concurrent
  session queued FIVE independent %1 GPU arrays (446357/446365/446366/446367/446370, no
  dependencies: their own potential concurrency = 5 > 4, a pre-existing breach not created
  here). Fix applied to OUR side only: `scontrol update JobId=446371
  Dependency=afterany:446357:446365:446366:446367:446370` — the whole chain
  (scaffold **446371** → bake **446372** → train **446373** %3 → eval **446374** %3,
  afterany each) now starts only after their queue drains; our standalone peak is 3 ≤ 4. KS-ref caveat: the plain-base retain90 KS reference
  is reused for the scaf pool (no scaffold-specific oracle exists; fq is
  non-discriminative at 20-row forget sets anyway — deletion read from ppl/rouge + Δmu).
  Storage: ~63 GB new (scaffolded base + pool) against 265 GB free after the user-approved
  226 GB nmerge-merges cleanup (dirs `nmerge_r32{,_e25,_centered}/merges` deleted
  2026-07-20; manifests/results/logs kept; rebuildable via `merge_subset.py`).
- **Results:** *CANCELLED same-day on user request before any task ran* — the whole chain
  (446371/446372/446373/446374) was still pending behind the concurrent session's queue
  when scancel'd; 0 GPU-h spent, no artifacts created (only the empty pool dir + copied KS
  ref remain). H-scaf-k200-1..3 stay OPEN/unrun; the pre-registration above remains valid
  for a future launch (`bash submit_k200_scaf.sh all`, re-derive the cap sum first).
- **What worked / hypothesis verdict:** *(pending)*
- **Observations:** *(pending)*
- **New questions / new hypotheses:** *(pending)*
- **Next Steps:** monitor; results entry resolving H-scaf-k200-1..3; if H2 confirms,
  extended+multi-seed on the new headline; if it refutes, the "scaffold = expert quality,
  not OOD competence" scope statement goes into the thread README.
