### Target Date: 2026-07-24 (Waves 4–5: shared ceiling, exhaustive families, ppl-native seal, per-rung disclosure)
- **Hypotheses / what we're testing:** Pre-registered 2026-07-23 ([wave-4 prereg](2026-07-23_hdet-refuted-wave4-prereg.md), [ledger](README.md)). **H-CEIL** — a router-INDEPENDENT `expert_max` ceiling de-degenerates activation_norm's ρ (its magnet misroutes even with no drop ⇒ ceiling≈floor). **H-EXH** — attn_norm + logit_div complete the per-family table (6 of 9). **H-SEAL-PPL** — a ppl-native margin seal (no stored identity sentinel) will MISS replicated facts *because* ppl is content-seeking: CONFIRM if sealed ρ@R8 ≥ 0.5 at ~0.9 generic-orphan catch; REFUTE if it collapses toward the author-tombstone's 0.047. **H-DISC-RUNG** — disclosure scales with catch (author catches 0.963 ⇒ discloses ≥ shard's 0.839; name catches 0.703 ⇒ less), i.e. catch and disclosure are one signal read twice. **H-MIA-ROUTER** (still running) — is the router leak visible to composed MIA, or blind like `fq`?
- **Setup:** Llama-3.2-1B scaffolded base, `_experts_scaf_k10` pool, `_entangled_k10` planted arm, `configs/entangled_facts_1b.json`, k=10, drop shard 9, seed 42, 200 planted facts (50 each at R=1/2/4/8). Drivers `submit_router_wave4.sh` (`ceiling|families|seal|riders`) + collector `collect_wave45.py`. SLURM: 448051 shared ceiling (`--channels expert_max --drop_shard none`), 448052 attn_norm, 448053 logit_div, 448054 ppl-seal (`--embed_abstain_tau 0.5473`), 448059 per-rung disclosure (`routing_audit_tofu --centroid_mode`, new out path — existing `rl_centroid_k10.*` untouched), 448060 MIA rider, 448073 CPU collector (`afterany`, so it reports `missing` rather than failing). τ=0.5473 = **retain** margin p1 from `rl_family_k10_behavioral.ppl.npz` (forget data never touched); reproduces the published ppl operating point (90% catch → retain FPR 0.010). ρ recomputed for ALL families against the shared ceiling via `aggregate_rho.py --ceiling_channel expert_max`. CPU gates green throughout (test_router_leak, test_entangled_facts, test_router_family, test_routing_audit_tofu, test_deletion_audit, analyze_router_family --self_test 15/15).
- **Results:** Shared ceiling = **0.9193**; denominators healthy (0.776–0.799) for every family. ρ@R8 verbatim (post / floor): **ppl 0.937** (0.871/0.142), attn_norm 0.905 (0.844/0.126), activation_norm 0.901 (0.842/0.137), centroid_sbert 0.882 (0.825/0.121), centroid_lm 0.813 (0.774/0.144), **logit_div 0.016** (0.156/0.144). ppl-seal: ρ@R8 **0.000**, probe-abstain rate **0.84**, R-profile 0.002 / **0.637** / 0.006 / 0.000 (R1/R2/R4/R8). Disclosure AUC by rung: shard **0.839** (catch 0.605), name **0.967** (catch 0.703), author **0.987** (catch 0.963). MIA rider pending.
- **What worked / hypothesis verdict:** **H-CEIL SUPPORTED** — activation_norm now reads a sensible 0.901; the Wave-3 value was degenerate (ρ=1.0 at *every* R including R=1, impossible for a single-owner fact). All Wave-3 per-family ρ are superseded. **H-EXH SUPPORTED** — table complete at 6 families. **H-DISC-RUNG SUPPORTED** — catch and disclosure rank-correlate perfectly (shard 0.605→0.839, name 0.703→0.967, author 0.963→0.987): **no free seal**. **H-SEAL-PPL REFUTED at the pre-registered bar** (ρ@R8 0.000 « 0.5) **but NOT safely claimable** — the R-profile is non-monotone (R2 0.637 against ~0 elsewhere) whereas every unsealed family rises monotonically in R, so the shape is anomalous; 50 facts/R makes noise plausible. Recorded as refuted-with-caveat, not as a working seal. **H-MIA-ROUTER** open.
- **Observations:** The headline is that **the leak is a property of the scoring signal, not of routing per se.** `logit_div` — which scores by distance from the candidate-set mean, i.e. picks the most *atypical* expert — is a genuine non-leaker: its post sits at the never-trained floor (0.156 vs 0.144) on a healthy denominator, ~57× below ppl. That is the same axis the H-DET refutation exposed a day earlier (content-seeking vs geometric), now with a clean negative control: **content-seeking ⇒ privacy leak; outlier-seeking ⇒ no privacy leak** (integrity failure only, per the Wave-2 confabulation result). Practical correction to our own recommendation: the **name rung**, advertised as privacy-cleanest because it stores only a name embedding, discloses at 0.967 — nearly as loud as the author rung's 0.987. Clean in storage ≠ clean in behaviour. Silent-failure checks: all denominators non-degenerate; no NaNs; the collector is `afterany`-chained and lists missing inputs explicitly.
- **New questions / new hypotheses:** (1) Explain or dismiss the ppl-seal R2 spike — rerun with more facts/R, or inspect the per-fact abstain decisions at R2; until then the router-native seal is unproven. (2) The seal's **retain-traffic utility cost** is unmeasured (0.84 abstain on probes is correct behaviour there, but the retain FPR at serve time was only calibrated, never served). (3) Does `logit_div`'s non-leak survive at k=200 / other pools, and does it cost utility (it was never a strong router)? If it holds, "route by atypicality" is a *design* lever for leak-free routing. (4) Extend Mode-B to the remaining 3 families (centroid_lm_last, centroid_sbert_q, key_*).
- **Next Steps:** Collector 448073 writes `reports/WAVE45_COLLECTED_2026-07-24.md` + `wave45_summary.json` autonomously; fill the H-MIA-ROUTER row from it. Then decide between the ppl-seal R2 follow-up and the τ-thresholded tombstone serving arm (still the highest-value unrun item).

---
**Addendum (2026-07-24, MIA rider landed — H-MIA-ROUTER):** composed-model MIA on the served
embed-routed arms — sibling (leak) AUC **0.072–0.182**, tombstone (seal) 0.220–0.284 across
loss/min_k/min_k++/zlib. Both **below** the oracle floor (0.379). **H-MIA-ROUTER SUPPORTED:** the
router leak is invisible to MIA, just as `fq` is blind to it. Sub-floor values are mechanistic —
a sibling serves neither forget nor holdout gold, so members look *less* likely than non-members,
pushing AUC <0.5. Two independent leak detectors (`fq`, MIA) both miss the routing channel; only
the content/ρ probes see it. Jobs 448060 (MIA) + 448073 (CPU collector) ran autonomously;
numbers in `reports/WAVE45_COLLECTED_2026-07-24.md` + `results/router_leak/wave45_summary.json`.
Campaign complete.

---
**Addendum (2026-07-24, ppl-seal R2 spike diagnosed — CPU, from the served JSON):** the
non-monotone profile is a real mechanism, not noise. Abstain rate by R: R1 0.90 / **R2 0.52** /
R4 0.96 / R8 0.98. The non-abstaining R2 facts route to a fact-HOST **88%** of the time with a
LARGE mean margin (**1.19** ≫ τ=0.5473) and recall prob ≈0.87. Mechanism: at R2 the deletion
leaves exactly ONE surviving cohost, which is *distinctively* the least-perplexed expert for the
fact ⇒ large ppl margin ⇒ seal does NOT abstain ⇒ routes straight to the leaker. At R4/R8 several
cohosts all fit ⇒ no single standout ⇒ margin < τ ⇒ abstain (for the wrong reason). **Verdict
upgraded from "refuted-at-bar/not-claimable" to: the ppl-native seal is STRUCTURALLY BROKEN,
most blind at R2 — the single-cohost case, the most common real deletion scenario — and the
aggregate ρ@R8=0.000 masked it.** Generalizes H1's confidence-detector refutation: a confidence
signal peaks exactly when one distinctive survivor holds the fact, i.e. weakest where privacy
risk is sharpest. A router-native margin seal is NOT a substitute for the identity tombstone.
