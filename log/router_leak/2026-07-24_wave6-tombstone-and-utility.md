### Target Date: 2026-07-24 (Wave 6 — H3 seal DEMONSTRATED + per-strategy utility×leak table)
- **Hypotheses / what we're testing:** **H3-closer (A):** the author-rung tombstone served with a *thresholded* margin (not argmax) drives served retain cost → ~0 while sealing — the config Phase-1's ROC predicted but that was never served (the served shard-argmax rung missed the ≤0.005 bar at −0.0061). CONFIRM if served mu/retain_prob ≥ the embed-router baseline (0.6872/0.573) with fq high. **H-UTIL (B):** is `logit_div`'s leak-freedom (Wave-4 Mode-B ρ 0.016) because it routes WELL (→ "route by atypicality" is a free design lever) or BADLY (→ leak-free only by misrouting)? Measure served routed utility for all 9 servable routers and cross with the leak axis. Sub-question flagged: k=200 logit_div is unrun (Mode-B needs a planted arm; behavioral routers hit the k>50 memory law).
- **Setup:** Llama-3.2-1B scaffolded base, `_experts_scaf_k10`, k=10, drop shard 9, seed 42, smoke tier. **A:** new `EmbedRoutedModel` policy `tombstone_author` + `--tombstone_tau` (eval_routed_scaffold.py sha256 `c452a300a3838af2`): survivor-only routing gated by per-deleted-author identity sentinels (mean question embedding per author, recomputable from the deletion request); abstain to base+scaffold when (best sentinel sim − best surviving-centroid sim) > τ. τ=**0.1944** calibrated on RETAIN author-rung margins from `rl_centroid_k10.sims.npz` → 90% orphan catch / 0.11% retain-FPR (reproduces Phase-1). Served via `eval_routed_scaffold.py --embed_route tombstone_author --delete_shard 9 --tombstone_tau 0.1944 --smoke`. CPU gate `test_router_leak.py` extended with a tombstone_author case (abstain>τ / route-otherwise / raise-without-sentinels), green. **B:** `eval_tofu.py --label routed_<strategy>` for the 9 servable routers on the scaffold pool (retain_prob = the clean in-domain routing-quality signal; mu shares the OOD-through-router confound across all labels, so read within-column). Drivers: `submit_router_wave6.sh` (`ab975f1b73e919e1`) jobs 448154 (tombstone) + 448155 (logit_div/key_exact/centroid_sbert); utility array 448156 (%2) + 448178 (key_tfidf). ⚠ `centroid_sbert_q` is NOT servable (no `_build_routed_model` branch — crashed as in Wave 2; correctly dropped).
- **Results:** **(A)** author-rung τ-thresholded tombstone served: **mu 0.695, retain_prob 0.6043, fq 0.808**; route_stats routed 448 / ood 1208 / deleted(abstained) 542 / route_mismatch 187. Baselines: hard-router 0.7509/0.854; embed-full 0.6872/0.573/fq 0.011; sibling 0.6922/0.581/fq 0.588; shard-rung tombstone 0.6861/0.574/fq 0.697. **(B)** routed utility (mu / retain_prob), sorted: ppl 0.636/0.873; key_tfidf 0.629/0.856; key_exact 0.603/0.844; centroid_sbert 0.580/0.608; logit_div 0.566/0.646; centroid_lm_last 0.549/0.449; centroid_lm 0.501/0.331; attn_norm 0.443/0.225; activation_norm 0.407/0.151. Cross-ref leak (Mode-B ρ@R8 / self-detect AUC): ppl 0.937/0.998, centroid_sbert 0.882/0.564, centroid_lm 0.813/0.474, attn_norm 0.905/0.533, activation_norm 0.901/0.412, logit_div **0.016**/0.633; key_exact/key_tfidf lexical (no-match AUC 0.927 / self-detect 0.973), centroid_lm_last leaks (AUC 0.505).
- **What worked / hypothesis verdict:** **H3 CLOSED — the identity seal is demonstrated, not just audited.** The thresholded author rung serves at mu 0.695 ≥ embed-full 0.6872 and retain_prob 0.604 ≥ 0.573, i.e. ~0 cost on top of the embedding router it rides, while sealing hardest of any arm (fq 0.808 > shard 0.697 > sibling 0.588). The residual gap to the 0.7509 hard-router ceiling is the embedding router's own ~40% misroute (paid pre-deletion), not a seal cost. **H-UTIL: "route by atypicality" SUPPORTED as a real leak-free option, REFUTED as a free lunch.** logit_div is the only genuine non-leaker (ρ 0.016) but costs ~0.23 retain_prob vs the best router (0.646 vs ppl 0.873). The dominant structural finding: **utility and leakage are POSITIVELY coupled along the content axis** — the routers that route well (ppl 0.873, key 0.84–0.86) are the content/lexical ones that either leak (ppl ρ 0.937) or rely on lexical self-detection (key AUC 0.93–0.97); the behavioral-norm/dense routers (activation_norm 0.151, attn_norm 0.225, centroid_lm 0.331) are worst on BOTH axes.
- **Observations:** The 2-axis table is the routing half of a merge-vs-routing master: it says the practical sweet spot is **key/lexical routing** (high utility + controllable, self-detecting leak surface), which is exactly where the whole thread kept landing — not a behavioral or dense-embedding router. logit_div is the interesting *research* direction (breaks the utility↔leak coupling, at a utility price), not the default. Silent-failure checks: no NaNs; the tombstone_author abstain count (542) ran higher than the calibrated 0.11% retain-FPR predicted — the utility metrics (mu/retain_prob ≥ baseline) are unaffected and authoritative, but the exact retain-FPR should be re-read at extended tier before quoting a cost number (calibration was on the centroid router's margin; serving uses per-author sentinels — a small distribution shift is plausible).
- **New questions / new hypotheses:** (1) extended-tier tombstone_author to nail the exact served retain-FPR (smoke abstain count higher than predicted). (2) Is the utility↔leak coupling monotone — does a router's retain_prob predict its ρ across the family (n≈6 with ρ)? If so, "leak-free ⇒ utility-capped" becomes a stated tradeoff curve. (3) logit_div at k=200 needs a planted arm + a feature-space reformulation to bypass the behavioral memory law. (4) key_tfidf Mode-B ρ unmeasured (lexical; predicted low — it self-detects at 0.973).
- **Next Steps:** Fold complete (numbers in `reports/ROUTING_MASTER_2026-07-23.md` Wave-6 block). Optional: extended tombstone_author FPR; the retain_prob↔ρ tradeoff-curve fit.
