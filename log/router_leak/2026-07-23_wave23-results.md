### Target Date: 2026-07-23 (Wave 2/3 results — per-strategy sibling-content + per-family Mode-B ρ)
- **Hypotheses / what we're testing:** Follow-up GPU waves to the same-day master-table pass ([2026-07-23_master-table-orphan-metrics](2026-07-23_master-table-orphan-metrics.md)), filling the two genuine measurement gaps. **H-W2 (content generality):** the "router leak is confabulation, not disclosure" result (prior: MiniLM centroid sibling, sibling-vs-gold 0.277 ≈ floor 0.249, confab 0.955) holds for EVERY router family — CONFIRM if sibling-vs-deleted-gold ≈ floor across all 9 servable strategies; REFUTE if any strategy's sibling discloses (sib-vs-gold ≫ floor). **H-W3 (Mode-B per family):** unsealed, every dense/behavioral router surfaces the replicated-fact leak (prior: single centroid router ρ 0.833 @R8) — CONFIRM if verbatim ρ rises monotone in R to ≳0.8 @R8 for the leaky families; watch for degeneracy where a magnet router's ceiling≈floor.
- **Setup:** Driver `submit_router_wave23.sh` (sha256 `e23806000ca2e46f`), chained serially behind sepmlp under the 4-GPU cap. Wave 2 = `dump_generations_routed.py --strategies` (sha256 `cfb68dd48f238a05`; new `--strategies` path: own+base+every-survivor generated once per orphan, cached, then each `router.py` strategy's routed sibling read off the cache via `merge_lora._build_routed_model`). Wave 3 = `eval_entangled_probe.py --embed_strategy` (sha256 `26252aa1582b6bfc`; new per-family `served_embedsim` routing) over ceiling(planted,no-drop)/post(planted,drop9)/floor(clean,drop9) → `aggregate_rho.py`. Pools: `_experts_scaf_k10` (Llama-3.2-1B scaffolded base), `_entangled_k10` planted arm, `configs/entangled_facts_1b.json`, seed 42, k=10, forget shard 9 (400 orphans). SLURM jobs: w3 centroid_sbert 448009 / centroid_lm 448010 / ppl 448011 (first pass); activation_norm 448026 + w2 content 448025 (second pass after fixes). Result JSONs under `_experts_scaf_k10/results/router_leak/{sibling_content_by_strategy.json, rho_embedsim_fam_*.json}`.
- **Results:** **Wave 2** (400 orphans, floor base-vs-gold 0.249): sibling-vs-deleted-gold by strategy — key_exact 0.274, key_tfidf 0.286, centroid_sbert 0.279, centroid_lm 0.291, centroid_lm_last 0.286, ppl 0.279, activation_norm 0.288, attn_norm 0.265, logit_div 0.287; confabulation rate 0.938–0.978; busiest-sibling reproduces the magnets (centroid_lm_last s4 304/400, centroid_lm s4 260, activation_norm s6 326, attn_norm s3 165; key_exact s0 400 fallback; diffuse routers spread). **Wave 3** (`served_embedsim_prob` verbatim ρ by R): centroid_sbert 0.00/0.16/0.48/**0.972**; centroid_lm 0.00/0.22/0.86/**0.929**; ppl 0.01/0.97/0.98/**1.00**; activation_norm 1.00/1.00/1.00/1.00 (degenerate). Paraphrase-surface ρ@R8 0.07–0.15.
- **What worked / hypothesis verdict:** **H-W2 SUPPORTED** — every strategy's sibling answer sits at the base floor (max 0.291 vs 0.249; +0.02–0.04, well under the +0.05 disclosure bar) with confab ≥ 0.938; the leak is an integrity failure across the whole router family, not disclosure. **H-W3 SUPPORTED (with a degeneracy caveat)** — centroid_sbert/centroid_lm/ppl verbatim ρ rise monotone in R to 0.93–1.0 @R8; ppl is worst (ρ 0.97 already at R2 — it routes to the least-perplexed = fact-holding expert). activation_norm ρ is degenerate: its single-magnet misrouting sends probes to a non-host expert even with no drop, collapsing ceiling≈floor so the (post−floor)/(ceiling−floor) ratio is unreliable (reads 1.0 even at R1, which is impossible for a single-owner fact) — read qualitatively, not as a leak measurement.
- **Observations:** Two silent GPU failures caught by the polling loop and fixed mid-run: (1) `centroid_sbert_q` is a `router_family_audit`-only score with no `_build_routed_model` branch → `ValueError` at build time (dropped from the servable set); (2) the activation routers' `input_ids` were built on CPU while the model is on cuda → `RuntimeError: two devices cuda:0 and cpu` (moved to `next(model.parameters()).device` at both call sites). Both are exactly the silent-failure class CLAUDE.md §4 warns about; the smoke-first design + the loop's file-presence check surfaced them before they polluted the report. The paraphrase-vs-verbatim gap (0.10 vs 0.93 @R8) says the embed route leaks the exact planted string far more than its paraphrase — the router matches surface form.
- **New questions / new hypotheses:** (1) ppl's dual role — the same confidence signal self-detects orphans (AUC 0.998) AND makes its unsealed Mode-B leak the most severe; is a ppl-native seal (route-to-base when min-perplexity margin is low) strictly better than the MiniLM tombstone here? (2) fix the activation_norm ρ degeneracy with an oracle-host ceiling (route the ceiling to the true host rather than via the magnet router) to get a real leak number. (3) does per-author landing determinism (H-DET) predict host-hit rate under Mode-B? (4) attn_norm/logit_div Mode-B not run (representative subset was 4 families).
- **Next Steps:** Fold complete (numbers in `reports/ROUTING_MASTER_2026-07-23.md` Wave-2/3 tables). Optional: ppl-native seal arm; oracle-host ceiling for the magnet routers; extend Mode-B to attn_norm/logit_div.
