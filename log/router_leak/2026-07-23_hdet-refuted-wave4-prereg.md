### Target Date: 2026-07-23 (H-DET→host-hit REFUTED; two orthogonal leak channels; Wave-4 pre-registration)
- **Hypotheses / what we're testing:** Two things. **(a) H-DET→host-hit (tested here):** does per-author landing determinism (the new Wave-0 metric) predict the Mode-B *host-hit rate* (fraction of planted probes landing on a shard that actually holds a copy)? Prediction: CONFIRM if the high-determinism magnet routers (activation_norm 0.817, centroid_lm 0.692) show the highest host-hit and the ranking correlates positively; REFUTE if determinism and host-hit are uncorrelated or inverted. **(b) Wave-4 PRE-REGISTRATION (results not yet in):** **H-SEAL-PPL** — a *router-native* seal that abstains on ppl's own best-vs-runner-up margin (no stored identity sentinel, unlike the tombstone) will **fail precisely where privacy matters**: because ppl is content-seeking, a replicated fact's surviving host fits *well* ⇒ large margin ⇒ no abstain. CONFIRM if sealed ρ@R8 stays ≥0.5 while generic-orphan catch is ≈0.9; REFUTE if ρ collapses the way the author-rung tombstone did (0.833→0.047). Also pre-registered: **H-CEIL** (a router-independent `expert_max` ceiling de-degenerates activation_norm's ρ — its magnet misroutes even with no drop, collapsing ceiling≈floor) and **H-EXH** (attn_norm + logit_div complete the per-family table).
- **Setup:** (a) CPU only, no new inference: joined the Wave-3 `postdrop_embedsim_fam_*.json` per-fact routes (`served_embedsim_route_orig`) against `plant_manifest.json` `hosts[].shard`; host-hit computed over R>1 facts only (R=1 has no host copy by construction — the control). Determinism values from the Wave-0 `--sims_glob` pass. (b) Wave-4 code: `router.py` (sha256 `cc5ffb193f47a34a`) gains `PplRouter.score_candidates()` — an additive, behaviour-preserving refactor (`route` now delegates to it) exposing the per-candidate NLLs so the margin can be read without re-implementing scoring; `eval_entangled_probe.py` (`5183f05169dcbba4`) gains `--embed_abstain_tau`; `aggregate_rho.py` (`363ae0537fdd6837`) gains `--ceiling_channel` (remaps a different-channel ceiling onto the post channel). τ = **0.5473** = the **retain** margin p1 from `rl_family_k10_behavioral.ppl.npz` (forget data never touched); it reproduces the Wave-0 published operating point exactly (90% orphan catch → retain FPR 0.010). Driver `submit_router_wave4.sh` (`bff60cfbc2ec9398`), SLURM 448051 (shared ceiling) → 448052 (attn_norm) → 448053 (logit_div) → 448054 (ppl-seal), serial, chained behind the concurrent `ctv-w5-eval` array 448044 under the global 4-GPU cap. CPU gates green: test_router_leak, test_entangled_facts, test_router_family, analyze_router_family --self_test 15/15.
- **Results:** Host-hit rate (R>1) vs determinism — activation_norm det 0.817 / host-hit **0.500** (R2 0.080, R4 0.420, R8 1.000); centroid_lm 0.692 / **0.460** (0.120/0.340/0.920); centroid_sbert 0.595 / **0.460** (0.120/0.320/0.940); **ppl 0.432 / 0.853** (R2 **0.700**, R4 0.860, R8 1.000). Kendall τ(determinism, host-hit) = **−0.167** (2 concordant, 3 discordant, n=4).
- **What worked / hypothesis verdict:** **H-DET→host-hit REFUTED.** The ranking is if anything inverted: the *least* deterministic router (ppl, 0.432) has by far the *highest* host-hit (0.853), and the most deterministic (activation_norm, 0.817) sits at 0.500. The discriminating regime is R=2, where ppl hits a host on **0.700** of probes vs **0.080–0.120** for the geometric routers — a 6–9× gap.
- **Observations:** This separates the leak into **two orthogonal channels**, which explains the Wave-2/3 results better than the single "magnet" story did: (i) **geometric concentration** — dense/LM/activation routers pool orphans onto 1–2 magnet experts (high determinism, low n_eff); the consequence is an *integrity* failure (Wave 2: sibling-vs-gold at floor, confabulation 0.94–0.98). (ii) **content-seeking** — ppl routes by "which expert is least surprised by this text", which *is* the expert that memorized the fact; the consequence is a *privacy* leak (highest host-hit, Wave-3 ρ 1.00, ρ 0.97 already at R2). Geometry and content are not the same axis, and the dangerous one for replicated facts is content. Caveat: n=4 families, so τ is directional only, not a significance claim; the R2 contrast (0.700 vs 0.080) is the robust part.
- **New questions / new hypotheses:** (1) H-SEAL-PPL above — if confirmed, the sharp statement is "the router's own confidence is a good seal for *generic* orphans and a bad one for *replicated facts*", i.e. self-detection and privacy are not the same guarantee. (2) Does host-hit predict ρ better than determinism does (the mechanistic claim)? Testable once attn_norm/logit_div land (n=6). (3) Do the geometric routers' ρ values drop once the degenerate ceiling is fixed (H-CEIL)?
- **Next Steps:** Wave-4 chain 448051–448054; then recompute ρ for ALL families against the shared `expert_max` ceiling (`aggregate_rho.py --ceiling_channel expert_max`), read H-SEAL-PPL/H-CEIL/H-EXH, and fold into `reports/ROUTING_MASTER_2026-07-23.md`.
