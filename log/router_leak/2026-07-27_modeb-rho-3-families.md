### Target Date: 2026-07-27 (Mode-B ρ for the 3 unrun selector families)
- **Hypotheses / what we're testing:** Fill the three empty Mode-B ρ@R8 cells in the deletion-under-selector table (`centroid_lm_last`, `key_tfidf`, `key_exact`) — the label-free selectors that the Wave-3/4/5 sweep never ran (`collect_wave45.py:24` hardcodes only 6 families). Prediction from the signal-type pattern: content/geometric routers leak the shared fact (ρ 0.7–0.9 like the other centroid/content families); the lexical fallback (`key_exact`) does NOT (routes orphans to the shard-0 default, which holds no fact ⇒ ρ≈0).
- **Setup:** Reused the existing planted pool `checkpoints/Llama-3.2-1B-Instruct_entangled_k10` (`plant_manifest.json`, 10 host shards) and the already-computed router-independent shared ceiling `results/entangled/ceiling_expert_max.json` (0.9193) — NO replanting/retraining. Per family, mirrored `submit_router_wave4.sh:80-90`: post (`eval_entangled_probe.py --experts_dir <ent> --channels served_embedsim --embed_policy sibling --embed_strategy <fam> --drop_shard 9 --surface both`) + floor (`--experts_dir <clean scaf_k10>`) → `aggregate_rho.py --ceiling ceiling_expert_max.json --ceiling_channel expert_max`. Self-contained sbatch (scratchpad `rho_2fam.sbatch` / `rho_keyexact.sbatch`), SLURM **jobs 448803** (centroid_lm_last, key_tfidf, %2) + **448816** (key_exact, %1), 1B, seed 42, cap-safe (queue empty, 0/4 rl). CPU gates green: `test_router_leak.py`, `test_entangled_facts.py`. Probe passes `tokenizer`+`dataset` to `_build_routed_model` (eval_entangled_probe.py:167), so all 3 strategies construct (key_exact/key_tfidf need `dataset=`, centroid_lm_last needs both).
- **Results:** ρ@R8_verbatim (shared `expert_max` ceiling): **centroid_lm_last 0.7423** (post 0.720 / floor 0.147 / ceiling 0.919), **key_tfidf 0.7803** (post 0.746 / floor 0.129), **key_exact 0.0000** (0.0 at every R1–R8, verbatim and paraphrase). Scale validation: re-deriving `centroid_lm` with the identical shared-ceiling method reproduces the **published 0.8127** exactly (its own on-disk `rho_embedsim_fam_centroid_lm.json` uses the *per-family* ceiling → the superseded 0.9295, not comparable).
- **What worked / hypothesis verdict:** **SUPPORTED.** The two content/geometric routers leak (0.74 / 0.78, in-band with centroid_sbert 0.882 / centroid_lm 0.813 / ppl 0.937); the lexical fallback is a clean non-leaker (0.00, sharper than logit_div's 0.016). Signal-type → leak pattern holds across all 9 selectors now. New table narrative: keyed selection *refuses* the shared fact (key_exact/SISA ρ 0.00), label-free content/geometric selection *leaks* it (ρ up to 0.94).
- **Observations:** **Reconciliation caught a near-miss** — `rl_family_leak_table.md` shows centroid_lm_last 0.0147 / key_tfidf 0.0022 in a column that is NOT Mode-B ρ (it is **retain selection-shift**; centroid_lm's cell there is 0.0758 ≠ its ρ 0.813). The actual "Mode-B ρ@R8" column lives in `ROUTING_MASTER_2026-07-23.md` Table 6(B), where these three correctly carried `—`. No contradiction; the 50× gap was a column mismatch. key_exact's busiest-survivor (100%†, all orphans → shard-0) and its ρ (0.00) are coherent: the fallback concentrates but holds no fact.
- **New questions / new hypotheses:** The 7 published-design ρ (LegoNet/RAMoLE = ~1–2 GPU-days serving the planted pool through their own routers; ClAMU/SEA/MemAdapters = bespoke planted builds per substrate, ~2 wk) remain the only unrun cells — deferred by user decision. IA³ ρ ≈ 0 by construction (author-key → base), stated analytically not measured.
- **Next Steps:** Filled `ROUTING_MASTER_2026-07-23.md` Table 6(B) cells (key_tfidf 0.78 / key_exact 0.00 / centroid_lm_last 0.74) and the paper's `tab:route-fails`. Artifacts live at `_experts_scaf_k10/results/router_leak/rho_embedsim_fam_{centroid_lm_last,key_tfidf,key_exact}.json`. Optional: regenerate `WAVE45_COLLECTED` to fold these into the 9-family summary.
