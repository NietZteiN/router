### Target Date: 2026-07-23 (Wave 0 — routing master table + per-strategy orphan metrics, CPU)
- **Hypotheses / what we're testing:** Mostly a consolidation/measurement pass (no GPU), but with one falsifiable new-metric prediction. **H-DET (new):** a *per-author landing-determinism* scalar — for each deleted author, the fraction of its ~20 orphan questions that land on ONE surviving unit — tracks the magnet/diffuse split already seen in the aggregate `n_eff`. Prediction: CONFIRM if the n_eff≤2.1 "magnet" routers (activation_norm, centroid_lm, centroid_lm_last) score determinism ≥ 0.65 while the n_eff≥5.5 "diffuse" routers (ppl, attn_norm) score ≤ 0.50; REFUTE if determinism is flat across the two regimes (i.e. aggregate concentration is a pooling artifact, not a per-author one). Secondary (engineering): the widened analyzers must regenerate `orphan_destinations.{md,csv}` and `rl_family_leak_table.md` as a **superset** of the committed reports (no value drift), so the master table can consolidate every routing method × every stored orphan metric.
- **Setup:** CPU-only, no SLURM. Env `/home/jack/anaconda3/envs/test-env/bin/python`. Edited `analyze_orphan_destinations.py` (sha256 `bacefc90b789d58a`): widened the `--out_md` table with `top3_share`+`gini` (already in `concentration()`/CSV, dropped only by the md formatter) and added `per_author_determinism()` + `--sims_glob` (reads the router-family `.<strategy>.npz` sidecars — `scores`/`author_of_q`/`is_forget` — masks the dropped shard columns via the `get_author_shard` inverse `author//(200//k)`, argmax over survivors, per-deleted-author top-1 concentration). `analyze_router_family.py` (sha256 `0c7d4aa8ed9decb9`) already emitted the full per-strategy leak table (retain-shift/adequacy/best-conf AUC+FPR@90/no-match/tomb-author) — re-ran, no edit. Inputs (all pre-existing on disk, jobs 446563–446568): `rl_family_k10_{feature,behavioral}.json` + per-strategy `.npz`, `rl_family_k200.json` + `.npz`, `rl_enc_{mpnet,bge}.json`, `rl_centroid_k10.json`, legonet `rl_audit_{basepin,basepin_f01,basepin_f05,ft}.json`, `rl_routerlora_drop_s{42,43,44}.json`, `rl_enc_roc_*.json`. Deliverables written to `tofu_sisa_lora/reports/`: NEW `ROUTING_MASTER_2026-07-23.md`; regen `orphan_destinations.{md,csv}`, `rl_family_leak_table.md`; patched `METHODS_ROUTING_VS_MERGING.md` (model-label + pointer).
- **Results:** Per-author determinism (k=10 d9, mean over 20 deleted authors): centroid_lm_last **0.823**, activation_norm **0.817**, centroid_lm **0.692**, centroid_sbert_q 0.605, centroid_sbert 0.595, key_tfidf 0.508, logit_div 0.508, attn_norm **0.465**, ppl **0.432**; k=200 centroid_sbert 0.527 / key_tfidf 0.505 / centroid_lm 0.423. CSV regen = strict superset of the committed report (44 → 46 rows: +legonet forget01/forget05 trajectory; all shared rows byte-identical, cross-asserts pass). Leak table regen: 14 family entries / 38 cells, H-ARCH REFUTED (2 families separable). CPU gates: `analyze_router_family.py --self_test` 15/15 PASS, `test_router_family.py` ALL OK, `test_router_leak.py` ALL OK. All master-table markdown tables column-count-consistent.
- **What worked / hypothesis verdict:** **H-DET SUPPORTED.** Magnet routers score 0.69–0.82 determinism, diffuse routers 0.43–0.47 — the split reproduces `n_eff` at the per-author level (centroid_lm_last 0.823/n_eff 1.7 vs ppl 0.432/n_eff 7.0). So the "magnet" is genuinely per-author: under a hidden-state router one deleted author's whole question set funnels to the *same* surviving expert, not just the population aggregate. Engineering goal met — supersets regenerated with no drift, master reference consolidated.
- **Observations:** The regenerated `rl_family_leak_table.md` shows the author-rung tombstone catch/FPR is populated for the FEATURE strategies (centroid_lm 0.980/**0.974**, centroid_lm_last 1.000/**0.997**, centroid_sbert 0.975/0.390, key_tfidf 0.955/0.041) but `—` for the BEHAVIORAL ones (ppl/activation_norm/attn_norm/logit_div) — their sidecars carry no per-author sentinel scores, so the tombstone rungs for that family are genuinely unmeasured (not zero). The LM-hidden-state routers' tomb retain-FPR 0.97–1.0 quantifies "breaks the tombstone" (dense sentinels match retain queries too). No NaNs / out-of-bounds AUC.
- **New questions / new hypotheses:** (1) behavioral-strategy tombstone rungs (shard/author/name) need a `router_family_audit.py` re-run that also dumps sentinel scores — Wave 1. (2) sibling-content ROUGE is measured only for the MiniLM centroid sibling — per-strategy disclosure-vs-confabulation is Wave 2. (3) Mode-B ρ is measured only for the centroid router — per-family ρ is Wave 3. (4) Does per-author determinism predict Mode-B host-hit rate (a high-determinism magnet should deterministically hit the same host)? Test alongside Wave 3.
- **Next Steps:** Wave 1 — extend `router_family_audit.py` to dump per-author + name sentinels for behavioral strategies; run on k=10/n=32/k=200 under the 4-GPU cap (chained behind the live sepmlp jobs). Then Waves 2–3. Fold each into `ROUTING_MASTER_2026-07-23.md` (cells marked *(Wave N)* today).
