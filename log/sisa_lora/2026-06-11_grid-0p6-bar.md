### Target Date: 2026-06-11 (overnight grid results — 0.6 bar PASSED on smoke; extended confirmation)
- **Goal / Hypothesis:** Read the overnight k=1 LoRA recipe grid (7 variants, jobs 433518–433524) and apply the mu ≥ 0.6 gate under the corrected OU-faithful eval.
- **Setup:** Grid per 2026-06-10 entry (seed 42, bs1×ga32, max_len 256, smoke eval per variant). All 8 chained jobs COMPLETED overnight (last eval 09:50, collect 433525 wrote all_metrics_smoke.csv). Extended confirmation chain submitted today: **433550** prepare_eval --extended (KS ref, 120 truth rows) → **433551** winner extended eval ∥ **433552** base extended eval (`submit_llama2_extended_winner.sh`).
- **Results (smoke, metrics_version ou-2026-06-10):**

| variant | mu | retain_prob/rouge | real_rouge | world_rouge | fp/rp |
|---|---|---|---|---|---|
| base (no ft) | 0.4179 | 0.16 / 0.43 | 0.982 | 0.933 | 15.2 / 15.7 |
| A control lr2e-4 e3 r8 | 0.6277 | 0.54 / 0.57 | 0.839 | 0.887 | 2.11 / 2.13 |
| lr1e-4 e5 r8 | 0.6137 | 0.51 / 0.57 | 0.748 | 0.897 | 2.24 / 2.26 |
| lr5e-5 e5 r16 | 0.6134 | 0.51 / 0.57 | 0.795 | 0.877 | 2.26 / 2.28 |
| lr1e-4 e5 r16 | 0.6684 | 0.66 / 0.62 | 0.785 | 0.887 | 1.72 / 1.76 |
| lr2e-4 e5 r16 | 0.7208 | 0.90 / 0.80 | 0.865 | 0.900 | 1.30 / 1.32 |
| lr1e-4 e10 r16 | 0.7137 | 0.99 / 0.98 | 0.805 | 0.907 | 1.16 / 1.17 |
| lr5e-5 e10 r32 | 0.7392 | 0.99 / 0.98 | 0.835 | 0.917 | 1.15 / 1.17 |
| **lr1e-4 e5 r32 (WINNER)** | **0.7435** | 0.92 / 0.86 | 0.865 | 0.920 | 1.27 / 1.29 |

- **Observations:**
  - **All 7 variants + control ≥ 0.6 on smoke** — the old 0.2507 was purely a broken-metric artifact; even the control recipe passes under the corrected eval (0.6277).
  - Utility scales with adapter capacity × training exposure: r32 > r16 > r8 at fixed lr/epochs; e10 ≈ r32-at-e5. Winner 0.7435 approaches the locuslab full-FT reference (0.748 under our eval) and beats the OU leaderboard 0.63.
  - Mild real-authors degradation from ft (real_rouge 0.98→0.84–0.87) but no collapse; world facts essentially intact. forget_ppl ≈ retain_ppl for all (k=1 sees everything — no forgetting), forget_quality ≈ 0 as expected.
  - All 9 hmean components > 0 for every variant; no NaNs; sanity checks pass.
- **Next Steps:** Read 433551/433552 extended JSONs (~3.5 h): **pass = winner extended mu ≥ 0.6**. Then final table (base vs control vs winner, smoke+extended) + close out. Follow-up candidates (not blocking): r64 probe to see if the capacity curve saturates; refresh remaining 5 models' ft-vs-base under corrected metric (deferred scope).

**Update (2026-06-11 midday — SISA shard-recipe star grid launched):**
- New task: establish the *merged-all-shards baseline* under the corrected metric and freeze a default shard recipe. Baseline = merge all k shards (`merged_*`); unlearn = drop shard k−1 (`remerge_*`). Prior merged numbers (0.17–0.28) are pre-port artifacts.
- Star grid around center (k10, r32/α64, e5, lr1e-4 — transferred from the k=1 winner), seed 42, bs1×ga16 (repo shard convention), smoke eval only, 5 labels/config (merged/remerge × linear/dare_ties + shard_{k-1}_only; +cat for CENTER). Axes: rank (r8), epochs (e10/r16), lr (2e-4), k (4 and 20). CTRL = existing k10 r8/e3/lr2e-4 shards, eval-only.
- GPU constraint (user): sprint3 only (4 GPUs), arrays %4. SLURM: CTRL eval 433553; trainings 433554/433556/433558/433560/433562/433564 (+KHI prep 433565); evals 433555/433557/433559/433561/433563/433566; collect 433567. Script: `submit_shard_grid.sh` (sha256 to be recorded at decision time with the results).
- Caveats: k=4 forget_quality NaN by design (retain90 oracle invalid: forget shard = authors 150–199); k=10 configs reuse the _ft smoke KS reference (identical forget rows); k=20 preps its own (oracle valid).
- Decision rule: winner = highest merged mu (bar ≥ 0.6) with remerge mu within ~0.05 and forget_ppl → base (15.2); ties → higher k. Then freeze defaults in `train_lora_shard.py` + `submit_overnight.sh`.

**Update (2026-06-11 afternoon — extended gate PASSED, k=1 thread closed):**
- Winner `lr1e4_e5_r32` extended (jobs 433550–433551): **model_utility 0.7404** (smoke 0.7435 — agreement within 0.003; smoke caps are trustworthy at this scale). Components: retain_prob 0.925, retain_rouge 0.865, real_rouge 0.827, world_rouge 0.895, forget_ppl 1.27 ≈ retain_ppl 1.29, forget_quality 0.0 (trained on forget10 — expected).
- Base extended (433552): mu 0.4348 (smoke 0.4179). Final k=1 verdict: **LoRA ft lifts utility 0.43 → 0.74**, matching the locuslab full-FT reference (0.748) and beating the OU leaderboard finetuned value (0.63). Gate mu ≥ 0.6: **PASSED on smoke AND extended.**
- k=1 recipe of record: lr 1e-4, 5 epochs, rank 32/α64, bs1×ga32, max_len 256, seed 42.

**Update (2026-06-11 evening — 8 new merge methods implemented + data-required eval wiring):**
- **Goal:** extend the merge-method space (user request): DELLA/MAGPRUNE, Model Breadcrumbs, true KnOTS (shared-basis align→TIES), TSV-M (whitened task singular vectors), SLERP (pairwise → tree labels), `subtract_orth` (project merged delta ⊥ forget subspaces), Fisher, LoraHub (nevergrad CMA-ES). Plan: `~/.claude/plans/what-other-lora-averaging-cached-minsky.md`.
- **Setup:** new `merge_extra.py` (implementations) + registry/dispatch in `merge_lora.py` (`EXPERIMENTAL_MERGE_METHODS`, `DATA_REQUIRED_METHODS`, `label_requires_data`); `eval_tofu.py` now builds a dataloader for regmean/fisher/lorahub labels (`build_merge_dataloader`; remerge_* excludes forget data → SISA-safe) — this also fixes `regmean` being CLI-unusable. nevergrad 1.0.12 installed in test-env. New CPU regression suite `test_merge_extra.py` (tiny Llama, k=3, rslora): **all 13 test groups pass** (identity/limit equivalences vs PEFT linear & magnitude_prune, KnOTS/TSV/SLERP closed-form identities ≤1e-3, subtract_orth forget-subspace leak <1e-4, Fisher identity 2.9e-7, della/lorahub determinism). `test_ou_equivalence.py` still green.
- **Results (finding):** identity tests exposed a **merge scale convention split**: shards train with rslora ⇒ PEFT's merge scaffold has scaling √r≈2.83 (not 1), so ALL existing PEFT factor-space merges (linear/ties/dare_*/magnitude_prune/cat) produce effective deltas inflated ×√r vs the true weighted average; `weighted_avg_ab`/`weighted_ba` sit at ≈0.5×. Kept della_*/breadcrumbs bit-compatible with the PEFT family (comparability); knots/tsv/slerp/subtract_orth/fisher/lorahub are true-scale. Documented in repo CLAUDE.md (Key Design Invariants) — cross-convention comparisons need care.
- **Setup (GPU smoke):** SLURM job **433642** (array 0-5%2, sprint1-3, 1 GPU/task, ≤55 min): labels merged_della_ties, merged_knots_ties, merged_tsv, subtract_orth, merged_fisher, tree_root_slerp on `checkpoints/Llama-3.2-1B-Instruct` (--smoke; has post-port retain_tr_scores.npy). Cap %2 to stay clear of the star-grid queue (433553-433567).
- **Next Steps:** read 433642 JSONs; sanity vs existing merged_dare_ties (no NaNs, in-range mu/forget_ppl). If sane → regenerate manifests (`prepare_eval.py --smoke`) per model and fold the new labels into the standard sweeps; extended runs for fisher/lorahub. Backlog: LoRA-LEGO; decide whether a true-scale variant of the PEFT family is worth a controlled A/B.

**Update (2026-06-11 — Phase M launched: new merge methods + full metric reporting):**
- Grid verdict so far: merge method, not shard recipe, is the bottleneck (merged dare_ties ≈ 0.45–0.48 @k10, 0.54 @k4 vs k=1 ft 0.74; recipe axes flat). PEFT `linear`/`cat` pathological — root cause per tofu_sisa_lora/CLAUDE.md merge-scale note: rsLoRA makes the PEFT factor-space family produce sqrt(r)-inflated deltas.
- Phase M: evaluating the parallel session's new methods (della_linear/della_ties/breadcrumbs/knots_ties/fisher/lorahub, merged+remerge, + subtract_orth; tsv/slerp excluded — degenerate on 1B validation) on KLO (k4_r32_e5_lr1e4) and CENTER (k10_r32_e5_lr1e4). 31 smoke evals, sprint3 %4.
- New: `train_lora_shard.py --retain_authors N` (default 180, backward-compatible) so KLO gets a VALID forget_quality via a retain75 oracle (authors 0–149; k=4 forget shard = 150–199); KLO Phase A labels re-run for fq. tofu_sisa_lora/CLAUDE.md updated same-change.
- SLURM: oracle **433698** → prepare **433699** → KLO evals **433700** (18 labels); CENTER evals **433701** (13); collect **433702**. `test_merge_extra.py` green pre-submit; `submit_merge_methods_eval.sh` stub-verified.
- Reporting upgrade (user request): all tables now show, for merged AND remerge states: model_utility; forget side fq/ppl/rouge/truth_ratio; retain side prob/rouge/ppl/truth_scaled.

**Update (2026-06-11 night — new-merge smoke results, jobs 433642 + 433682 baselines):**
- **Results** (Llama-3.2-1B, r8/e3 shards, --smoke; mu / forget_quality / forget_ppl / retain_ppl):
  - `merged_della_ties` **0.4291** / 0.393 / 10.19 / 10.14 — beats `merged_dare_ties` (0.4236 / 0.393 / 13.22 / 13.24) on utility AND ppl. MAGPRUNE > random drop, as the DELLA paper claims.
  - `merged_knots_ties` 0.4242, `merged_fisher` 0.4239 — both match dare_ties within noise (single seed; variance check pending).
  - `subtract_orth` **0.4328 / 0.594** / 13.49 / 9.87 — highest utility on the board, highest forget_quality among non-degenerate labels, and the desired forget>retain ppl asymmetry. Compare `subtract_linear`: total collapse (mu 0.0, ppl ~3e5). Orthogonal projection of the forget subspaces is a far better subtraction operator and is O(1) on an existing merge — directly on-thesis.
  - `merged_tsv` 0.0514 and `tree_root_slerp` 0.0895 collapse real_authors (real_rouge ~0.01) — but so does `merged_linear` (0.0497, real_rouge 0.0067): this is the known un-sparsified-merge failure mode, not an implementation bug (tsv/slerp preserve the full common direction like linear; the TIES/DELLA/Fisher selection step is what protects real-world knowledge). Follow-up: density/weight sweeps or tsv+TIES hybrid.
  - All 9 labels: no NaNs, no empty generations, metrics in range. forget_quality ties at 0.5941 are smoke-cap KS quantization (truth≤30), expected.
- **Provenance:** SLURM 433642 (6 new labels) + 433682 (3 baselines), `eval_manifest_newmerge*.txt` in `results/smoke/`, seed 42 shards, eval rng 0, test-env.
- **Next Steps:** (1) seed/variance re-runs before claiming della>dare; (2) `remerge_*` + extended for della_ties/knots_ties/fisher/subtract_orth; (3) sweep `tsv` weights (1/n) and slerp t; (4) lorahub/regmean extended (data-required path now CLI-wired); (5) consider promoting `subtract_orth` to a headline unlearning operator next to remerge.

**Update (2026-06-11 evening — Phase M complete; REPORT + frozen defaults):**
- Full report: `tofu_sisa_lora/reports/SHARD_GRID_REPORT_2026-06-11.md` (grid + merge methods + qualitative examples + decision). Aggregate CSV refreshed by jobs 433567/433702. Script hashes: da8b12328f45 submit_shard_grid.sh;734950c3f8f7 submit_merge_methods_eval.sh;a107e38f2721 train_lora_shard.py;
- Phase M results (KLO k4 / CENTER k10, merged mu): lorahub **0.592**/0.492 (best), dare_ties 0.545/0.477, della_ties 0.497/0.500 (⚠️ k4 remerge collapse 0.236), knots_ties 0.489/0.463, fisher 0.477/0.456, subtract_orth 0.523/0.494 (remerge-only); della_linear/breadcrumbs degenerate (ppl 10⁴⁺, rsLoRA scale family).
- Unlearning verdict (3-part test): exact-by-construction; behaviorally decisive only at k=4 + dare_ties (f_ppl 4.5→11.9 vs base 15.2, TR→0.73≈base, retain improves). k≥10 trivially passes (dilution pre-forgets). fq at smoke caps separates only memorizers — ppl/TR deltas are the discriminative signal.
- Qualitative (job 433716, KLO): memorizer recites forget answers verbatim (R=1.0); merged confabulates partially; remerge confabulates with zero trace of true facts; base refuses/denies. Retain + world facts intact post-drop.
- **Frozen defaults (same-change edits):** train_lora_shard.py + submit_overnight.sh now default to r32/α64/e5/lr1e-4 (submit_overnight gained an lr param); oracles documented to keep legacy r8 recipe explicitly; default merge = dare_ties (lorahub = utility-max alternative) — all documented in tofu_sisa_lora/CLAUDE.md.
- Open items handed to merge-methods thread: della_ties k4-remerge bug; della_linear/breadcrumbs rsLoRA degeneracy. Next levers if >0.6 merged is required: lorahub budget/per-layer weights, routing-based serving, extended-cap evals for fq power, seed-variance pass.

**Update (2026-06-11 night — k-scaling sweep launched: k ∈ {50, 100, 200} + k200-r1 smoke arm + auto backups):**
- **Goal:** extend the dilution law (report §1.2: merged mu 0.74→0.54→0.48→0.45 for k=1→4→10→20) to the hard maximum k=200 (1 author/shard; `200 % k == 0` → feasible k>20 are {25,40,50,100,200}), and get the first GPU datapoints for routing-based serving (`routed_key_exact[_no{k-1}]`) — the report §8 "utility/granularity frontier" + "routing-based serving" levers in one sweep. Hypotheses: merged utility keeps dilution-decaying while remerge deltas vanish ("free forgetting"); routing should be ~k-independent and is the O(1)-deletion ideal (k=200 ≈ one expert per author; delete = drop the adapter).
- **Setup:** 4 arms on Llama-2-7B-chat-hf, frozen recipe r32/α64/e5/lr1e-4 (bs1×ga16, len 256, seed 42), per-arm dirs `…_k50_r32_e5_lr1e4`, `…_k100_r32_e5_lr1e4`, `…_k200_r32_e5_lr1e4`, plus `…_k200_r1_e5_lr1e4` (r1/α2) as an always-runs smoke arm: exercises every 200-adapter mechanic at ~0.8 GB adapter memory and probes the rank axis (at r1 the rsLoRA √r merge inflation = 1 → `merged_linear` may un-break). Forget shard = k−1 ⊆ forget10 at all three k (k=50→authors 196-199, k=100→198-199 = forget01, k=200→author 199); retain90 oracle symlinked (legacy-r8, KHI precedent); k-specific KS refs via `prepare_eval.py` (k200 arms share one — identical forget rows + oracle). 7 labels/arm: merged/remerge × {linear, dare_ties} + shard_{k-1}_only + routed_key_exact[_no{k-1}].
- **Orchestration (new `submit_scale_grid.sh`, STUB-verified; 6-GPU user budget):** stage-1 = ONE array 0-552%6 (3 preps + 550 trains) → gate r1 (mechanics: 200 loads + 200-way dare_ties merge + count assert; PEFT load_adapter overhead at k=200 is unmeasured — gate logs s/adapter) → gate r32 (memory: ~26 GB adapters + 13.5 GB base on 46 GB A40; `expandable_segments`) → eval arrays A (k50+k100, 14 tasks %2), B (k200r1, %2), C (k200r32, %2) → collect. Peak = 6 GPUs at every stage. `kill_invalid_depend` off cluster-wide → gates scancel their dependents on failure (ids in `checkpoints/scale_state/`); a failing r32 gate auto-submits the r8 backup arm (`backup_r8` mode: train %6 after A+B → gate → eval) so k=200 numbers exist by morning either way.
- **SLURM:** stage1 433769; gate-r1 433770; gate-r32 433771; eval-A 433772; eval-B 433773; eval-C 433774; collect 433775. sha256(12): submit_scale_grid.sh 8e6de3f4b778; gate_scale_load.py 1aca9ec2f3df; router.py 2f2ec35e8b44; train_lora_shard.py/eval_tofu.py/prepare_eval.py unchanged from the 2026-06-11 grid entry.
- **Bug found & fixed pre-launch (CPU pre-flight):** `router.build_key_index` pooled all shard questions before `_extract_author_names`' ≥50% threshold → key index EMPTY for any shard with >2 authors → silent shard-0 fallback for every query. Routing had never produced a result JSON, so no prior numbers affected. Fixed to per-author extraction + per-shard union; measured `key_exact` routing accuracy 0.86-0.87 at k ∈ {10,50,100,200} (misses = TOFU's name-free "Who is the notable author born in…" questions → fallback; inherent to lexical routing, worth reporting alongside the routed metrics).
- **Caveats pre-registered:** fixed e5 ⇒ ~6 optimizer steps/shard at k=200 (vs ~62 at k=20) — undertraining confound accepted (steps-matched arm k50 e12 / k100 e25 / k200 e50 is the follow-up if high-k collapse appears); forget_quality at k=200 = KS over 20 rows (low power); single seed 42.
- **Next Steps (morning):** `sacct -j 433769-433775` (no FAILED/TIMEOUT except an intentional gate stop); gate logs → load-time s/adapter + peak GiB vs eval `--time`; 28 JSONs (or r8 fallbacks) present; refresh `all_metrics_smoke.csv`; build the k ∈ {1,4,10,20,50,100,200} frontier table/plot (merged/remerge mu, f_ppl, fq + routed accuracy-adjusted utility); judge whether routing holds utility where merging dilutes; check `merged_linear` @r1 vs the √r-inflation story; write the 2026-06-12 LOG entry.

