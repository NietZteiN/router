### Target Date: 2026-06-17 (JD compression — Phase 2 high-k mode-B builds; bring-current + stranded-eval triage)
- **Goal / Hypothesis:** Execute the JD thread's pending "mode-B build at k≥100 to show flat
  memory" step (Next Steps of the 2026-06-15 JD entry): JD-compress 100/200 trained Llama-2-7B
  shard adapters into a shared per-cluster basis on GPU, materialize keep-all (`merged`) and
  keep-all-but-forget (`remerge`) PEFT adapters, and eval them via `eval_tofu --preloaded_adapter`
  (loads base + 1 adapter → sidesteps the fp32 high-k memory wall that makes in-model JD impossible
  at this k). Hypothesis under test (eval pending): selective-keep deletion stays O(1) and utility
  survives compression even as recon error climbs at large k/c.
- **Setup:** new `submit_jd_highk.sh` (STUB-verified). Per k: `jd_collection.py build --device cuda`
  (cluster counts per paper §10.1: k100→c7, k200→c10; rank 16, seed 0) → `merge` keep-all → `merge
  --drop shard_{k-1}`. k=100 uses the r32 shard set (`…_k100_r32_e5_lr1e4`), k=200 the r8 set
  (`…_k200_r8_e5_lr1e4`, only rank that fits). Artifacts under `/storage2/jack/jd_collections/`
  (home is code-only). Builds: `--mem 80G`, `--time 02:30:00`, `PYTORCH_CUDA_ALLOC_CONF=
  expandable_segments:True`. Eval array `afterok:` both builds, `--array 0-3%4` (2 k100 + 2 k200
  labels: `{merged,remerge}_jd_full_c{c}`), ≤4 GPUs. seed 42 on eval, `--k {100,200}
  --forget_shard_id {k-1}`, smoke tier.
- **Results:**
  - **k100 (c7) BUILT and ready to eval.** Job 434863 → recon_err **0.9307** (~57 min); after a
    `jd_compress.py`/`jd_collection.py` update (15:5x) rebuild 434879 → recon_err **0.8695**
    (~42 min). `k100_c7_merged` + `k100_c7_remerge` PEFT adapters materialized (247 MB each;
    full collection 882 MB). Memory wall cleared as designed — build is the only GPU-heavy step.
  - **k200 (c10) NOT built.** Build 434880 hit the `#SBATCH --time 02:30:00` wall (CANCELLED DUE
    TO TIME LIMIT at 18:25); earlier k200 attempts 434864/434871/434877 were cancelled
    (superseded by code-update relaunches). One k100 attempt (434870) hit a CUDA OOM on the
    per-module SVD while sharing a GPU — fixed forward by `expandable_segments` + GPU exclusivity.
  - **Eval array 434881 STRANDED:** `tofu-jd-highk_[0-3%4]` is PENDING / `DependencyNeverSatisfied`
    because its `afterok` set includes the k200 build that timed out. No JD high-k metrics exist
    yet — eval has not run on any k.
- **Observations:** recon_err **0.87–0.93** at k100/c7 is far above the 0.62 seen at k10/c1 on
  TinyLlama and above the paper's 0.6/99%-perf threshold — compressing 100 7B adapters into 7
  clusters discards a lot of subspace, as expected when k/c is large (200/10 will be worse). The
  small 0.93→0.87 drop between the two k100 builds tracks the clustering-convergence code update.
  Whether that recon loss costs TOFU utility is exactly the open question the stranded eval would
  answer. The k200 build is the bottleneck: 200-adapter per-module JD-Full doesn't fit a 2.5 h
  wall on one GPU. NO results were fabricated for this entry — utility/forget_quality cells remain
  empty pending eval.
- **Next Steps:** (1) **k100 is unblocked now** — resubmit the eval as a 2-task k100-only array
  (its build deps satisfied), don't wait on k200; the `afterok:BID100:BID200` coupling is what
  stranded it. (2) k200 build: raise `--time` (≥6 h) and/or shard the per-module SVD across the
  array / drop to lower c, then re-chain its eval. (3) once k100 metrics land, fold high-k cells
  into the JD report alongside the k≤10 results and refresh `select_num_clusters` guidance for
  large collections.
- **Action (same day):** executed Next-Step (1). Cancelled stranded array 434881; added
  `submit_jd_highk_k100_eval.sh` (STUB-verified, no build, no dependency — evals the already-built
  `k100_c7_{merged,remerge}` artifacts via `--preloaded_adapter`, labels `{merged,remerge}_jd_full_c7`,
  writes to `…_k100_r32_e5_lr1e4/results/smoke/`). Submitted as array **435181** `[0-1%4]`, now
  PENDING/Priority (clean schedule wait, not DependencyNeverSatisfied). seed 42, --k 100
  --forget_shard_id 99, smoke. Read-out target: `merged_jd_full_c7.json` (utility under JD
  compression at k100) + `remerge_jd_full_c7.json` (selective-keep deletion of author 199). k200
  still owes a longer-wall rebuild before its eval.
- **Resolution (eval landed; sweep called at k=100 per user):** 435181 completed. **k100 c7
  metrics: `remerge_jd_full_c7` mu 0.465 / fq 0.239 / retain-RougeL 0.436; `merged_jd_full_c7` mu
  0.464 / fq 0.239.** Despite recon 0.87, utility is preserved and JD still **beats dare_ties** at
  k100 (mu 0.430 / fq 0.135) on both axes — confirming recon error is a loose proxy for downstream
  quality (paper's point; moderate-to-high recon ≠ ruined performance). merged≈remerge at k100
  because the forget shard is a single author (1/100) → fq non-discriminative there (expected KS
  power loss, not a JD weakness). **Full smoke curve (remerge_jd_full = selective-keep unlearn) vs
  the dare_ties baseline and the k=1 ft monolith:**
  | k | JD mu | JD fq | dare mu/fq |
  |---|---|---|---|
  | 1 (ft) | 0.628 | 0.016 | — (monolith can't forget) |
  | 4 | 0.515 | 0.808 | 0.575 / 0.808 |
  | 10 | 0.487 | 0.808 | 0.460 / 0.393 |
  | 50 | 0.486 | 0.393 | 0.438 / 0.239 |
  | 100 (c7) | 0.465 | 0.239 | 0.430 / 0.135 |
  Headline: 1-LoRA monolith = best utility but ~zero forgetting (fq 0.016); JD selective-keep
  forgets AND beats dare_ties on both utility and fq at every k≥10. fq declines with k only because
  the forget slice shrinks (k10=20 authors → k100=2). **k=200 dropped** (build > 2.5 h wall; its
  forget signal would be a single author = non-discriminative anyway — marginal value). All k≤50 and
  k100 cells are real smoke-tier, single seed.
- **Bugs fixed this thread (all real, regression-tested):** (1) clustering collapse → L2-normalize
  the k-means Σ features (raw Euclidean made large-norm adapters singletons: `[94,1,1,1,1,1,1]`);
  (2) build OOM → stream factors per-module to GPU (peak 37 GiB → ~2 GiB, ~flat in n); (3)
  empty-cluster dummy basis device-mismatch in `save_jd`. CLAUDE.md invariants + `eval_tofu
  --preloaded_adapter` flag documented.
- **Open follow-ups (not blocking):** TOFU author-shards are far less jointly compressible than the
  paper's task-LoRAs (recon 0.87 at k100/c7 vs the paper's <0.6 task-LoRA regime) — `select_num_clusters`
  guidance for TOFU likely needs c ≫ 7; extended-tier + seed-variance before any claim; k=200 only if
  a literal 200-point is wanted (raise build `--time` ≥6 h and/or cut the reassignment GPU-sync overhead).

