### Target Date: 2026-06-29 (GPU smoke green on Llama-3.2-1B; two submit-script bugs fixed)
- **Goal / Hypothesis:** Validate the SIFT-Masks pipeline end-to-end on the real base model
  (`meta-llama/Llama-3.2-1B-Instruct`) at T=5 before committing to the full T=200 run — i.e. confirm
  the model loads, `frozen_substr` selects the right trainable params, the fp32 footprint fits an A40,
  sign-fixed full-FT + streaming merge + deterministic unlearn run on GPU, and (the headline)
  `SiftMasksModel` serves correctly through the `eval_tofu --sift_masks_config` hook to produce valid
  OU metrics. Follow-up to [2026-06-29_implementation.md](2026-06-29_implementation.md).
- **Setup:** T=5 smoke config `configs/sift_masks_tofu_1b_smoke.json` (num_authors=5, steps=2,
  forget tag `forgetsmoke`=[4]). Driver `bash submit_sift_masks_tofu.sh configs/sift_masks_tofu_1b_smoke.json all`
  with `SIFT_LABELS="sift_full sift_unlearn"` (lean 2-label subset via the new env override). SLURM
  chain on sprint2: build 438646 → unlearn 438647 → eval 438648 → (collect 438649). Eval re-run after
  fixes: 439358. Determinism hardening added to `train_sift_masks.load_base`
  (`_force_deterministic_attention`: math SDPA backend + `attn_implementation="eager"`).
- **Results:**
  - **build** (438646): trainable_params **973,146,112** (≈0.97 B; embed+lm_head tied & frozen),
    5/5 tasks, wall **36.7 s**, ~0.6 B active mask entries/task. `sift/tau_bar.pt` (3.9 GB) +
    `sign_v.pt` (3.9 GB) + 5 packed masks written.
  - **unlearn** (438647): re-derived author 4, wrote `tau_bar_forgetsmoke.pt` + manifest (T: 5→4).
  - **eval** (439358, both labels, smoke caps): `sift_full` mu **0.4694** / fq **0.3929** /
    forget_tr 0.7681; `sift_unlearn` mu **0.4712** / fq **0.3929** / forget_tr 0.7681. No errors.
  - **Sanity:** fq 0.3929 ≡ the repo's known **base/SISA-1B forget_quality 0.393** — correct, since at
    T=5 the untrained forget10 authors route to base θ0 in both conditions, so the KS-vs-retain90-oracle
    path reproduces the base value. sift_full ≈ sift_unlearn is also expected at T=5 (the science needs T=200).
- **Observations:** The headline integration works — `SiftMasksModel` + `eval_tofu --sift_masks_config`
  yields a full OU metric dict on a real model. Two **submit-script bugs** surfaced and were fixed
  (both in the KS-reference handling):
  1. **forget_quality=NaN:** the reference was a *relative-target* symlink
     (`ln -s ./checkpoints/…`), which resolves from the link's own dir → broken →
     `os.path.exists` False → `retain_ref_tr_scores=None` → NaN. **Fix:** `cp -f` the 368-byte file
     (real, CWD-resolved). Verified loadable on CPU (shape (30,), finite) → fq went NaN→0.3929.
  2. **`sift_unlearn` failed:** the two parallel eval array tasks raced on `ln -s` and the loser
     aborted under `set -e` (`File exists`). The `cp -f` fix is idempotent/race-safe.
  Also: the build log warned *"Memory Efficient attention defaults to a non-deterministic algorithm"*
  — relevant to exact unlearning (GPU re-derivation of τ_u wouldn't byte-match), so `load_base` now
  forces the deterministic math/eager attention path. CPU exactness gate still green after all edits.
- **Next Steps:** Full run on user go-ahead: `bash submit_sift_masks_tofu.sh configs/sift_masks_tofu_1b.json all`
  (T=200; build ~30–60 min + eval). Expect the *science*: `sift_full` recovers utility vs the
  `merge_full` FT+Merge collapse; `sift_unlearn` keeps retain utility while fq rises; cross-check the
  paper's answer-probability via `eval_sift_masks.py` (held-in ≈ 0.99 at T=200). The T=5 smoke dir
  (`checkpoints/Llama-3.2-1B-Instruct_sift_masks_smoke`, ~12 GB on /storage2) is disposable.
