### Target Date: 2026-07-22 (P2 pilot — H1 REFUTED for v1; blocks near-empty, not question-keyed)
- **Hypotheses / what we're testing:** **H1 (make-or-break localization):** the disconnected block transcoder trained with plain LM + λ-warmed L1 suppression (DESIGN §15 v1 recipe) reaches SELECTIVE — median per-author on/off ≥5 (REFUTE <2; LoRA anchor 1.11 LAZY) with own-prob ≥0.80 — across an lr×λ pilot. Also probes **G3** (all-active vs own-only recall gap ≤0.05 = memsinks-interference tripwire) and the bs32 memory go/no-go.
- **Setup:** phase-0 shared block trained first (447419: 15 ep, author-free pool 2000 alpaca + 100 real_authors, train_loss 0.45→0.029, author blocks bitwise-pristine, peak 35.06 GiB @bs32, ckpt `runs/phase0_s42/blocktc.pt` 213 MB). Then P2 pilot array **447430** (`0-5%1`), 6 arms = lr {3e-4,1e-3,3e-3} × λ_max {0.01,0.1}, K=20 (authors_subset [0,20)), bs32, 15 ep, span 3, insert 9, m_author 32, m_shared 128, seed 42, λ warmup 0.15, detector_init 'questions', base Llama-3.2-1B-Instruct frozen (OU chat-template track). Each arm: `train_tc.py --config configs/<arm>.json` then `measure_selectivity.py --config … --recall_probe`. Gate: median per-author on/off ratio of mean **question-token** block act mass (LAZY<2 / SELECTIVE≥5); recall = OU answer-prob, all-active vs own-only. CPU gates 91-green pre-submit; queue ≤3 GPU (sepmlp 447384/385 + pilot %1) ≤ cap 4.
- **Results:** All 6 arms **LAZY**, and selectivity *anti*-correlates with lr (opposite of sepmlp). median on/off | recall(all-active) | train_loss: arm0 lr3e-4/λ.01 **0.711** | 0.086 | 1.37 · arm1 lr3e-4/λ.1 **0.407** | 0.083 | 1.35 · arm2 lr1e-3/λ.01 **0.204** | 0.002 | 2.99 · arm3 lr1e-3/λ.1 **0.000** | 0.003 | 3.01 · arm4 lr3e-3/λ.01 **0.000** | 0.002 | 3.77 · arm5 lr3e-3/λ.1 **0.000** | 0.002 | 3.45. Best arm (0): frac authors on/off<2 = **0.90**, ratio min 0.000 / max 6.918. Recall probe (arm 0), per-author split: the 20 **trained** authors [0–19] own-only prob ≈ **1e-5** (≈0, *below* base) but all-active 0.12–0.33; the 180 **untrained** authors [20–199] own-only ≈ 0.005–0.11 (≈ base, decoders still zero-init). Mean all-active 0.0855 vs own-only 0.0212, gap +0.0642 → G3 TRIPPED (arms 0–1); at higher lr all-active collapses <0.003 so the "gap" clears trivially. Memory **37.04 GiB @ bs32** every arm (fits 45 GiB A40). Training telemetry (arm 0, epoch 15): own_mass 155.5 / off_mass 15.4 (~10:1 across blocks, all tokens) vs probe on/off 0.711 (question tokens, across authors).
- **What worked / hypothesis verdict:** **H1 REFUTED for the v1 (plain LM + L1) recipe** — median on/off 0.711 (best) to 0.000, frac<2 ≥0.90, none near the ≥5 bar; own-prob far below 0.80. Operationally clean: bs32 fits (37 GiB), phase-0/phase-1 pipeline healthy, shared block bitwise-frozen, no NaN/OOM. But the localization the method depends on did **not** emerge from LM loss.
- **Observations:** The train-telemetry (own 10:1) vs probe (0.711) gap is the whole story: blocks fire strongly on their author's **answer** tokens (teacher-forced, so train_loss drops) but are **not question-keyed** — on question tokens (all the model has at inference) block k fires no more on its own author than on others (median 0.711, 90% <2). The recall split proves the knowledge is **not localized**: a trained author's OWN block active *alone* drives recall to ~0 (worse than base — the isolated decoder writes push the residual off-distribution), while all-active gives only 0.12–0.33. This is **memsinks failure mode #2 reproduced** (near-empty per-author slices, weak collective-only recall; the exact prior blocktc was meant to beat) plus the "stored but question-keyed" retrieval failure — the #1 pre-registered failure mode (DESIGN §9), and span-3 cross-layer decoding did NOT rescue it at K=20. Higher lr makes decoder writes diverge (loss↑ 1.37→3.8, recall→0). Two plausibly-compounding causes, both pre-registered: (a) objective — plain LM loss doesn't manufacture question-keyed firing; (b) capacity — 32 features at one site (vs sepmlp's 32×16-layer banks that reached own-prob 0.977) may be below the memsinks capacity floor. Silent-failure checks: no NaN, loss finite, save/reload parity held; own-only=0.0212 constant is REAL (untrained blocks = base), not a probe bug. Storage /storage2 at 97% (249G free) — pilot ckpts ~1.3 GB, flagged.
- **New questions / new hypotheses:** Does sepmlp's **promotion loss** (force ≥1 own detector to fire on own *question* tokens) + hinge/Gram suppression — the DESIGN §15 pre-registered fallback, and the exact mechanism that gave sepmlp selectivity 4.38–1909.7 — convert blocktc's answer-token firing into question-keyed firing? Is the low collective recall (0.12–0.33) an objective problem or a capacity floor (test with m=64 width ablation)? Would a lower lr than 3e-4 (where recall was best 0.086 but still LAZY) or a lower insertion layer help? Is the single-site bet simply wrong for gated branches (sepmlp needed all 16 layers)?
- **Next Steps:** Ladder halted at P2 for a human decision — v1 refuted, and the natural fix (v2 = adopt sepmlp's promotion + hinge/Gram recipe) is a substantive objective change, not a resubmit. Recommended: **implement v2 promotion recipe** (port sepmlp's L2 hinge w10 / L3 Gram w50 / L4 promotion w1 on own-question-tokens into `train_tc.py`, keep the transcoder architecture + exactness masking unchanged) and re-run the K=20 pilot as a clean A/B vs this v1 baseline; in parallel the width ablation (m=64) tests the capacity hypothesis. Do NOT commit GPU to v2 without user go-ahead. sepmlp context: its parallel thread CONFIRMED the promotion-recipe mechanism reaches selectivity but hit a K=200 recall ceiling ≈0.80 — relevant prior for blocktc v2's own K-scaling.
