### Target Date: 2026-07-22 (P1 smoke green — pipeline validated end-to-end on GPU)
- **Hypotheses / what we're testing:** Not a scientific hypothesis run — this is the pre-registered **P1 smoke gate** (see [2026-07-21 pre-registration](2026-07-21_preregistration-and-build.md)). Pass condition, stated before the run: the full-size transcoder (F=6528) trains phase-0→phase-1 in one job on GPU with (a) all `debug_grad_check` grad-structure asserts passing, (b) shared block bitwise-frozen through phase 1 and author blocks bitwise-pristine after phase 0, (c) save→reload bitwise parity, (d) finite loss / no NaN / no OOM, (e) a bs-representative peak-mem print that clears the bs32 pilot. This gate makes NO selectivity/utility claim (5 steps/phase, untrained).
- **Setup:** `submit_blocktc.sh smoke` → `#SBATCH --gres=gpu:1 --exclude=sprint4 --mem=48G`, A40 (46068 MiB), test-env `/home/jack/anaconda3/envs/test-env/bin/python train_tc.py --config configs/smoke.json --smoke`; seed 42, base `meta-llama/Llama-3.2-1B-Instruct` (frozen), insert 9 / span 3 / m_author 32 / m_shared 128 / n_authors 200, smoke `authors_subset [0,4)`, bs 8, max_steps 5/phase, alpaca_n 16, detector_init 'questions'. Provenance in `runs/smoke_s42_p0/meta.json` and `runs/smoke_s42_p1/meta.json` (config + script sha256, job id). SLURM jobs: **447413** (failed, guard bug — below), **447416** (`[smoke] PASS`). CPU gate before both: `pytest tests/ -q` = 91 passed / 1 GPU-skip. Queue at submit: sepmlp 447384 running + 447385 pending + smoke = 3 GPU ≤ cap 4.
- **Results:** 447416: `[smoke] PASS`. Phase 0 — `lm=2.3273 supp=0.0 lambda=None` (call 0), train_loss 1.266, wall 0.9 s, peak mem **6.50 GiB**, `[belt] author blocks bitwise-pristine`. Phase 1 — `lm=1.9793 supp=0.0` (call 0), train_loss 2.866, wall 0.9 s, peak mem **13.43 GiB**, `[belt] shared block bitwise-frozen vs phase-0 checkpoint`, `[smoke] reload + parity check PASS`. Phase-1 telemetry at 5 steps (NOT a result, just liveness): own_mass_mean 174.18, off_mass_mean 5.07, onoff_ratio_median 1.029, ood_over_own 0.0297. detector-init pointed 4/200 author blocks at question-mean directions. 447413: `AssertionError: distinct-author guard: saw 3 authors, expected 4 - source_ids plumbing is broken` at `train_tc.py:1144`.
- **What worked / hypothesis verdict:** **P1 smoke SUPPORTED** (447416 green on all five pass conditions). The 447413 failure was a **smoke-harness bug, not a plumbing bug**: the coverage guard asserted `real_seen == set(subset_ids)` (full-epoch coverage) after a `max_steps=5` run, but the alternating author/generic sampler fits only ~⌊5/2⌋=2–3 single-author batches into 5 steps, so ≤3 of the 4 subset authors can be visited — mathematically impossible to satisfy. Confirmed sound plumbing from the same 447413 log: phase-0 saw only `NO_AUTHOR`, phase-1 author batch single-source, `[grad-check:phase1-generic-lm] LM gradient exactly zero everywhere`, all grad-checks structurally OK. Fix (train_tc.py:1142–1150): split the guard into (i) `real_seen ⊆ subset` — the real plumbing invariant, holds at any step count, strictly stronger — and (ii) full-coverage `==` gated on `trainer.state.epoch ≥ 1.0`; a step-capped run only requires `real_seen` non-empty. Re-ran `pytest` (91 passed) then resubmitted → 447416 PASS.
- **Observations:** Silent-failure sweep clean — losses finite (no NaN), no OOM, no frozen loss, save→reload bitwise-identical. Peak mem **13.43 GiB @ bs8** on the full F=6528 transcoder (vs sepmlp's 14.04 GiB @ bs32 on the 16-layer bank) ⇒ the bs32 pilot comfortably clears the 45 GiB A40. The 5-step onoff_ratio_median 1.029 is at the LoRA-anchor LAZY floor (1.11) — **expected and meaningless here** (blocks are essentially untrained after 5 steps; selectivity is a P2 question). Two review fixes are baked into this build: OOD-probe/train-window overlap (probe head now 14000, disjoint) and the symmetric `decode()` grad-guard (unrouted grad-enabled forward with a trainable transcoder now fails loud).
- **New questions / new hypotheses:** Does H1 selectivity (median on/off ≥5) actually emerge under real training (15 epochs, K=20) where the LoRA anchor and memsinks priors predict it may not? What is the true bs32 peak mem (extrapolation from 13.43 GiB @ bs8)? Does the shared-block phase-0 absorption leave author blocks enough to memorize 20 QA pairs through the 32-feature bottleneck (capacity-floor risk from memsinks)?
- **Next Steps:** **P2 pilot pending user go-ahead** — 6 arms (lr {3e-4,1e-3,3e-3} × λ_max {0.01,0.1}), K=20, array `0-5%1`, gate G2 = leakage-matrix near-diagonal + median on/off ≥5 + own-prob ≥0.80. Two open decisions for the human: (1) whether sepmlp's own P2 pilot runs alongside (currently 447384/447385 occupy 2 GPUs; a blocktc `%1` pilot + sepmlp ≤2 keeps ≤3 under cap 4) — recommended, it's the cheapest test of the shared nonlinear-gate bet; (2) confirm K=20 pilot before the K=200 full run.
