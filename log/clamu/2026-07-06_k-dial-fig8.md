### Target Date: 2026-07-06 (K-dial E4/H5 + Fig-8 forgotten-serving H8-align)
- **Hypotheses / what we're testing:**
  - **H5 (K dial):** where does the storage–utility curve bend between Global merge (0.337–0.351)
    and SIFT per-task masks (0.737)? Sub-predictions: (a) K=1 optimized collapses to ≈Global;
    (b) some K < 200 matches SIFT's utility at a fraction of the mask storage.
  - **H8-align (Fig-8 serving):** serving forgotten authors the maskless RETAIN merge
    `θ0 + τ̄_<tag>/T` (papers' Fig-8 protocol) instead of raw θ0 raises extended fq toward the
    oracle-comparable range with mu unchanged. CONFIRM: fq ↑ materially, mu bit-unchanged.
- **Setup:** dial = 6 chains K∈{1,4,16,50,100,200} (`configs/clamu_tofu_1b_K{K}.json` →
  `{slug}_clamu_K{K}` dirs), **`mask_epochs=4`** (equal mask-training epochs across K via the
  new `clamu.localize_steps`; fixed steps would confound the dial), `heuristic_masks=false`,
  `mask_batch_rows=8`; `tau_bar.pt`/`tau_bar_forget10.pt`/`author_emb.npy` symlinked from the
  K=16 build (K-independent; build + unlearn-subtract skipped via new resume guards;
  `cluster_authors` caps K at the author count so the K=200 retain re-cluster = 180
  singletons). Labels `clamu_full`+`clamu_unlearn`, smoke tier, seed 42. Fig-8 =
  `configs/clamu_tofu_1b_fig8.json` (`forgotten_serve:"merged"`, same artifacts), extended
  eval of `clamu_unlearn`/`merge_unlearn` → `results/extended_fig8/`. SLURM: fig8 440433;
  dial chains 440434–440475. CPU gate 8/8 pre-submit (2 new tests: `localize_steps`,
  label→mask-kind dispatch).
- **Results:** (smoke; mu = model_utility; masks = du of the full-model mask dir)

  | K | full mu | unlearn mu | masks (MB) |
  |---|---|---|---|
  | 1 | 0.5519 | 0.5578 | 175 |
  | 4 | 0.6103 | 0.6036 | 704 |
  | 16 | 0.6620 | 0.6649 | 2,861 |
  | 50 | 0.6683 | 0.6509 | 9,026 |
  | 100 | 0.6571 | 0.6615 | 18,145 |
  | 200 | 0.6716 | 0.6716 | 36,377 |

  References (same path, smoke): Global merge 0.3511; 07-02 headline K=16 fixed-50-steps
  0.6469; SIFT per-task T=200 0.737; routing_scaffold weak-experts 0.556 / **strong-experts
  0.7509** ([routing_scaffold 07-06](../routing_scaffold/README.md)). Fig-8 (extended):
  `clamu_unlearn` fq **0.0045 → 0.0352**, mu **0.6204 → 0.6204** (unchanged); `merge_unlearn`
  fq 0.0045 → 0.0352, mu 0.3369 unchanged. No failures in any log.
- **What worked / hypothesis verdict:**
  - **H5 SUPPORTED, sub-prediction (a) REFUTED — the headline finding.** A **single optimized
    mask (K=1) reaches 0.552** — +0.20 over Global merge and ≈ weak-experts routing (0.556) at
    175 MB. Optimized masking does NOT collapse without clustering: **most of ClAMU's recovery
    is the mask *optimization*, not cluster specialization.** The dial then saturates early:
    K=16 (0.662) captures ~93% of the K=1→K=200 range; K=200 adds only +0.010 for 12.7× the
    storage. The knee is K≈4–16. Sub-prediction (b) REFUTED: no K matches SIFT — even
    optimized per-author masks (K=200, 0.672) stay below SIFT's sign-trained per-task masks
    (0.737) at the same granularity and storage, so **the mask training regime
    (sign-constrained FT) matters more than mask granularity beyond K≈16**.
  - **H8-align PARTIALLY SUPPORTED — same verdict as sift's H8.** Fig-8 serving raises fq 8×
    (0.0045→0.0352) with mu bit-unchanged, but not to the oracle range: the retain merge
    `τ̄/T` is diluted, so its truth-ratio style still differs from the oracle LoRA. Matches
    the sift thread's resolution (0.0045→0.0505, mu unchanged): **extended fq measures
    oracle-style match, not leakage** — cross-track confirmed on a second method.
  - Deletion is utility-preserving at **every** K (unlearn ≈ full throughout, ≤0.017 apart).
- **Observations:** the K=16 dial point (0.662, mask_epochs=4) slightly beats the 07-02
  fixed-50-step headline (0.647) — equal-epochs is also just a better recipe. The dial is
  non-monotone within noise (K=50 0.6683 > K=100 0.6571 < K=200 0.6716) — single-seed smoke;
  don't over-read ±0.01. Mask storage is ~179 MB/mask, linear in K, as expected for bit-packed
  fp32-param-shaped masks. The comparison frame moved again this week: routing with STRONG
  experts hit 0.7509 (best of all tracks), so ClAMU's beat-routing claim from 07-02 is now
  scoped to *matched-weak-experts*; ClAMU's dial ceiling (~0.67) sits below both SIFT (0.737)
  and strong-expert routing (0.751).
- **New questions / new hypotheses:**
  - **H9 (open):** does sign-constrained training + *optimized* cluster-level masks (SIFT-FT
    inside ClAMU's build) close the 0.672→0.737 gap at K=16-level storage? One build pass
    (`use_sign_constraint=True`) + the existing localize.
  - **H10 (open):** is K=1-optimized (0.552 @ 175 MB) the best *storage-constrained* point in
    the whole repo? Compare per-GB against JD-compressed merges and the scaffold.
  - H6 (why ClAMU>routing) is now **moot at the top end** (strong-expert routing 0.7509 wins);
    the surviving version: why does *optimization* recover what averaging destroys (ties to
    merge_mechanism's subspace-collision result)?
- **Next Steps:** treat the dial as characterized (single-seed caveat); fold the dial table +
  Fig-8 into `reports/CLAMU_REPORT_2026-07-02.md` (§7 addendum); mark H5/H8-align resolved in
  the thread README; H9 is the one cheap high-value follow-up if the thread continues.
