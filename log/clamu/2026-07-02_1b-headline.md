### Target Date: 2026-07-02 (1B headline — Global/EMR/TALL/ClAMU ladder + exact forget10)
- **Hypotheses / what we're testing:**
  - **H1 (ladder order).** Under the strict OU `model_utility`, the paper's localization ladder
    holds: Global merge < EMR < TALL < ClAMU-optimized. CONFIRM: monotone mu ordering with
    ClAMU clearly on top. REFUTE: heuristic masks ≈ optimized, or optimized ≈ Global merge.
  - **H2 (does cluster-masking survive the strict metric?).** ClAMU's paper reports only
    answer-probability; the risk is that optimized cluster-masking craters like every other
    merge (~0.35–0.45 band) once all 9 mu components are scored. CONFIRM (survives): clamu_full
    mu materially above the merge band, approaching route+scaffold (0.664). REFUTE: mu ≲ 0.45.
  - **H3 (exact deletion is utility-free).** forget10 deletion by τ_u-subtraction + retain
    re-cluster/re-localize preserves retain utility and moves forget signals toward base.
    CONFIRM: clamu_unlearn mu ≈ clamu_full, forget_rouge/ppl → base-served levels.
    REFUTE: mu drop or unchanged forget signals.
  - Also: does the localize OOM fix (mask_batch_rows=8 + expandable_segments; gradient
    checkpointing being incompatible with `functional_call`) get the STE stage through 1B?
- **Setup:** Llama-3.2-1B-Instruct, TOFU full (T=200 authors), K=16 feature clusters
  (MiniLM answer-mean k-means, kmeans_seed 42), no sign constraint, steps 20 / lr 1e-4 /
  seed 42 per task; STE localize: mask_steps 50, mask_lr 0.05, mask_opt_seed 42+c,
  **mask_batch_rows 8** (the OOM fix — first attempt job 439831 OOM'd at 44 GiB on the
  20-row LM-head logits; gradient checkpointing can't be used under `functional_call`, so
  the batch is capped instead; + `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` in the
  driver). Config [`configs/clamu_tofu_1b.json`](../../tofu_sisa_lora/configs/clamu_tofu_1b.json)
  (sha256:12 `000cc82d1240`); scripts `clamu.py` `e33b36a467ea`, `clamu_model.py`
  `8f7f56932ecd`, `train_clamu.py` `42685517155b`, `submit_clamu_tofu.sh` `9f2e2f30c12c`.
  Command: `bash submit_clamu_tofu.sh configs/clamu_tofu_1b.json all` (smoke tier,
  `--k 10 --forget_shard_id 9`, SISA retain90 KS reference). SLURM: first attempt
  439829 (setup) / 439830 (build, ~1 h, succeeded) / 439831 (localize, **OOM**);
  retry 440189–440195 (440190 build **skipped** via the new resume guard; localize 440191
  all 16 clusters in ~13 min; unlearn 440192; localize_tag 440193; eval array 440194;
  collect 440195). CPU gates re-run green before resubmit (`test_clamu.py` 6/6,
  `test_sift_masks.py` 6/6).
- **Results:** (smoke tier, seed 42; mu = model_utility, fq = forget_quality)

  | label | mu | fq | f_ppl | r_ppl | f_rouge | r_rouge | retain_prob |
  |---|---|---|---|---|---|---|---|
  | merge_full (Global, no mask) | 0.3511 | 0.5941 | 57.53 | 58.47 | 0.336 | 0.293 | 0.111 |
  | emr_full | 0.3882 | 0.808 | 43.19 | 46.46 | 0.356 | 0.320 | 0.135 |
  | tall_full (λ=0.4) | 0.4053 | 0.3929 | 17.28 | 17.46 | 0.360 | 0.352 | 0.155 |
  | **clamu_full (optimized)** | **0.6469** | 0.3929 | 16.45 | 16.28 | 0.525 | 0.527 | 0.459 |
  | **clamu_unlearn (exact forget10)** | **0.6609** | 0.3929 | 20.33 | 16.48 | 0.339 | **0.568** | 0.457 |
  | merge_unlearn | 0.3527 | 0.3929 | 20.33 | 58.95 | 0.339 | 0.302 | 0.110 |

  References on the SAME eval path (smoke, 1B, seed 42), per the same-day
  [routing_scaffold repro](../routing_scaffold/2026-07-02_scaffold-repro.md): routed+scaffold
  (OOD-aware) **0.556**, committed-code routed+scaffold 0.474, routing-only 0.458, full-FT
  (`_ft`, legacy r8) **0.530**, SIFT-Masks T=200 per-task masks **0.737**
  ([t200-results](../sift_masks/2026-07-02_t200-results.md)), FT+Merge no-mask 0.407, SISA
  merges ~0.43–0.48. (The ORIENTATION figures 0.664/0.599 were REFUTED by that repro — do not
  compare against them.) OOD components identical across all rows
  (real_rouge 0.952, world_rouge 0.953, real_prob 0.685, world_prob 0.754) — OOD routes to
  base θ0 in every condition by construction. Mask density ~54% (523–540 M of 0.97 B);
  16 packed masks ≈ 1.9 GB vs SIFT's 200 ≈ 24 GB. Localize wall ~13 min (16 clusters × 50
  steps); micro-smoke (TinyLlama, 06-29→07-01 run): clamu_full 0.4530 vs merge_full 0.4003,
  clamu_unlearn 0.4515.
- **What worked / hypothesis verdict:**
  - **H1 SUPPORTED.** 0.351 (Global) < 0.388 (EMR) < 0.405 (TALL) ≪ **0.647 (ClAMU)** —
    monotone, and the optimized mask nearly doubles Global-merge utility (+0.296). The gains
    are exactly where masking should act: retain_prob 0.111→0.459, retain_rouge 0.293→0.527,
    retain_ppl 58.5→16.3; heuristic masks recover almost none of that.
  - **H2 SUPPORTED (the surprise).** clamu_full **0.647** breaks far out of the merge band —
    and on the same eval path it **beats the reproduced route+scaffold (0.556) and full-FT
    (0.530)**, sitting between routing and SIFT's per-task masks (0.737). Optimized
    cluster-masking is the first *merging-side* method in this repo to overtake routing.
  - **H3 SUPPORTED.** clamu_unlearn mu **0.661 ≥ 0.647** (utility rises after deletion —
    the paper's interference effect), retain_rouge 0.527→0.568; forget signals move to the
    base-served level exactly (f_rouge 0.339 and f_ppl 20.33 ≡ merge_unlearn's base-served
    forget row, since forgotten authors route to θ0 by construction).
  - **OOM fix worked**: all 16 clusters localized at 1B under the 44 GiB card.
- **Observations:** fq must not be over-read at smoke tier: 0.3929 is the base-model value
  and appears for every condition whose forget-set serving ≡ base (clamu/tall full serve a
  diluted τ̄/200 that barely memorizes; unlearn conditions serve θ0 for forgotten authors by
  construction). EMR's fq 0.808 is *not* good forgetting — it's collateral damage (a broken
  model looks "never-trained"). mu differences are therefore driven entirely by retain-author
  components, which is the clean read. No NaNs; generations non-degenerate (rouge mid-range,
  not 0/1 spikes); losses finite throughout localize logs. The first-attempt OOM was a real
  memory-math failure (43.6 GiB PyTorch alloc), not a foreign-PID collision.
- **New questions / new hypotheses:**
  - **H4 (open):** does the extended-cap eval confirm the smoke ladder (esp. clamu_full
    0.647 and the unlearn ≥ full ordering)? Smoke KS/rouge caps are small.
  - **H5 (open):** the mask-granularity dial now reads Global K=1-equivalent (0.351) →
    cluster K=16 optimized (0.647) → per-task T=200 sign masks (SIFT 0.737). Where does it
    bend? Is there a K (50? 100?) matching SIFT's utility at a fraction of its 24 GB mask
    storage — and does K=1 optimized actually collapse to Global?
  - **H6 (open):** why does cluster-masking beat routing here (0.647 vs 0.556)? Candidates:
    the routed experts are the weak legacy-r8 shards vs ClAMU's full-FT deltas, and/or the
    optimized mask exploits cross-author signal a single expert can't. A stronger-experts
    routing rerun and/or a clamu+scaffold hybrid would decompose it.
  - **H7 (open):** random vs feature clustering at fixed K (the paper claims 12–30% for
    feature-based; the repo's route+scaffold found clustering barely matters — direct tension).
- **Next Steps:** extended-cap eval pass (`CLAMU_EVAL_ARGS=--extended CLAMU_EVAL_SUB=extended`
  … `eval` phase only, artifacts already on disk); write
  `reports/CLAMU_REPORT_2026-07-02.md` with the ladder + cost ledger; then the gated expansion
  (K dial E4 first — it doubles as H5 — then random-vs-feature E2/H7).
